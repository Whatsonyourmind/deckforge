---
phase: 01-foundation-ir-schema
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/deckforge/main.py
  - src/deckforge/api/__init__.py
  - src/deckforge/api/app.py
  - src/deckforge/api/deps.py
  - src/deckforge/api/middleware/__init__.py
  - src/deckforge/api/middleware/auth.py
  - src/deckforge/api/middleware/rate_limit.py
  - src/deckforge/api/routes/__init__.py
  - src/deckforge/api/routes/health.py
  - src/deckforge/api/routes/render.py
  - src/deckforge/api/routes/jobs.py
  - src/deckforge/api/schemas/__init__.py
  - src/deckforge/api/schemas/responses.py
  - src/deckforge/api/schemas/errors.py
  - src/deckforge/workers/__init__.py
  - src/deckforge/workers/settings.py
  - src/deckforge/workers/tasks.py
  - src/deckforge/workers/storage.py
  - src/deckforge/workers/webhooks.py
  - tests/integration/test_health.py
  - tests/integration/test_auth.py
  - tests/integration/test_rate_limit.py
  - tests/integration/test_openapi.py
  - tests/integration/test_idempotency.py
  - tests/integration/test_workers.py
  - tests/conftest.py
autonomous: true
requirements:
  - API-08
  - API-09
  - API-10
  - API-11
  - API-14
  - WORKER-01
  - WORKER-02
  - WORKER-03
  - WORKER-04
  - WORKER-05

must_haves:
  truths:
    - "GET /v1/health returns status with PostgreSQL and Redis connectivity checks"
    - "A request with a valid dk_live_ or dk_test_ API key passes authentication"
    - "A request with missing or invalid API key returns 401"
    - "Requests exceeding the tier rate limit (10/min starter, 60/min pro) return 429 with Retry-After header"
    - "GET /docs returns auto-generated OpenAPI 3.1 spec with all IR models documented"
    - "POST /v1/render with a duplicate request_id returns the existing result instead of re-processing"
    - "An async job submitted to ARQ is picked up by the correct worker queue"
    - "Job progress events are published to Redis pub/sub and retrievable"
    - "Webhook delivery fires httpx POST on job completion"
    - "Files upload to MinIO/S3 and a download URL is generated"
  artifacts:
    - path: "src/deckforge/main.py"
      provides: "FastAPI app factory with lifespan managing Redis and DB connections"
      exports: ["app"]
    - path: "src/deckforge/api/middleware/auth.py"
      provides: "API key authentication dependency"
      exports: ["get_api_key", "CurrentApiKey"]
    - path: "src/deckforge/api/middleware/rate_limit.py"
      provides: "Redis token bucket rate limiter"
      exports: ["check_rate_limit"]
    - path: "src/deckforge/api/routes/health.py"
      provides: "Health check endpoint"
      contains: "GET /v1/health"
    - path: "src/deckforge/api/routes/render.py"
      provides: "IR render stub endpoint that validates and echoes IR"
      contains: "POST /v1/render"
    - path: "src/deckforge/workers/settings.py"
      provides: "ContentWorkerSettings and RenderWorkerSettings for dual ARQ pools"
      exports: ["ContentWorkerSettings", "RenderWorkerSettings"]
    - path: "src/deckforge/workers/tasks.py"
      provides: "ARQ task functions for rendering and content generation"
      exports: ["render_presentation", "generate_content"]
    - path: "src/deckforge/workers/storage.py"
      provides: "S3/MinIO upload and presigned URL generation"
      exports: ["upload_file", "get_download_url"]
    - path: "src/deckforge/workers/webhooks.py"
      provides: "Async webhook delivery via httpx"
      exports: ["deliver_webhook"]
  key_links:
    - from: "src/deckforge/api/routes/render.py"
      to: "src/deckforge/ir/"
      via: "Validates request body against Presentation model"
      pattern: "Presentation"
    - from: "src/deckforge/api/middleware/auth.py"
      to: "src/deckforge/db/repositories/api_key.py"
      via: "Looks up hashed API key in database"
      pattern: "api_key_repo\\.get_by_hash"
    - from: "src/deckforge/api/middleware/rate_limit.py"
      to: "Redis"
      via: "Lua script atomic token bucket"
      pattern: "TOKEN_BUCKET_SCRIPT"
    - from: "src/deckforge/workers/tasks.py"
      to: "src/deckforge/workers/storage.py"
      via: "Uploads rendered file after job completion"
      pattern: "upload_file"
    - from: "src/deckforge/workers/tasks.py"
      to: "src/deckforge/workers/webhooks.py"
      via: "Fires webhook after job completion"
      pattern: "deliver_webhook"
    - from: "src/deckforge/main.py"
      to: "src/deckforge/api/routes/"
      via: "Includes all routers under /v1 prefix"
      pattern: "include_router"
---

<objective>
Build the FastAPI application skeleton with authentication, rate limiting, health checks, OpenAPI generation, idempotency, and the complete async worker infrastructure (ARQ dual pools, S3 storage, webhooks, job tracking).

Purpose: This wires together the IR schema (Plan 01) and infrastructure (Plan 02) into a running API server with auth, rate limiting, and async task processing -- completing all Phase 1 requirements.
Output: A running FastAPI server that accepts and validates IR payloads, authenticates API keys, rate limits by tier, generates OpenAPI docs, and processes async jobs through ARQ workers.
</objective>

<execution_context>
@C:/Users/lukep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-ir-schema/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

<!-- Depends on Plan 01 and Plan 02 outputs -->
@.planning/phases/01-foundation-ir-schema/01-01-SUMMARY.md
@.planning/phases/01-foundation-ir-schema/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from Plan 01 (IR Schema) -->

From src/deckforge/ir/__init__.py:
```python
from deckforge.ir.presentation import Presentation
from deckforge.ir.metadata import PresentationMetadata, GenerationOptions
from deckforge.ir.brand_kit import BrandKit
from deckforge.ir.slides import SlideUnion
from deckforge.ir.elements import ElementUnion
from deckforge.ir.charts import ChartUnion
```

From src/deckforge/ir/presentation.py:
```python
class Presentation(BaseModel):
    schema_version: Literal["1.0"] = "1.0"
    metadata: PresentationMetadata
    brand_kit: BrandKit | None = None
    theme: str = "executive-dark"
    slides: list[SlideUnion]
    generation_options: GenerationOptions | None = None
```

<!-- Key types from Plan 02 (Infrastructure) -->

From src/deckforge/config.py:
```python
class Settings(BaseSettings):
    DATABASE_URL: str
    REDIS_URL: str
    S3_ENDPOINT_URL: str
    S3_ACCESS_KEY: str
    S3_SECRET_KEY: str
    S3_BUCKET: str
    # ...
settings = Settings()
```

From src/deckforge/db/engine.py:
```python
async def get_db() -> AsyncGenerator[AsyncSession, None]: ...
```

From src/deckforge/db/repositories/__init__.py:
```python
api_key_repo = ApiKeyRepository()
deck_repo = DeckRepository()
job_repo = JobRepository()
```

From src/deckforge/db/models/:
```python
class User(Base): ...
class ApiKey(Base): ...  # key_hash, key_prefix, scopes, tier, is_active, is_test
class Deck(Base): ...    # request_id, ir_snapshot, status, file_url
class Job(Base): ...     # job_type, queue_name, status, progress
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: FastAPI app, auth, rate limiting, health, render stub, OpenAPI, idempotency</name>
  <files>
    src/deckforge/main.py
    src/deckforge/api/__init__.py
    src/deckforge/api/app.py
    src/deckforge/api/deps.py
    src/deckforge/api/middleware/__init__.py
    src/deckforge/api/middleware/auth.py
    src/deckforge/api/middleware/rate_limit.py
    src/deckforge/api/routes/__init__.py
    src/deckforge/api/routes/health.py
    src/deckforge/api/routes/render.py
    src/deckforge/api/routes/jobs.py
    src/deckforge/api/schemas/__init__.py
    src/deckforge/api/schemas/responses.py
    src/deckforge/api/schemas/errors.py
    tests/conftest.py
    tests/integration/test_health.py
    tests/integration/test_auth.py
    tests/integration/test_rate_limit.py
    tests/integration/test_openapi.py
    tests/integration/test_idempotency.py
  </files>
  <action>
    1. Create src/deckforge/api/schemas/errors.py with:
       - ErrorResponse(BaseModel): error: str, detail: str | None = None, request_id: str | None = None
       - ValidationErrorResponse(BaseModel): error: str = "validation_error", detail: list[dict]

    2. Create src/deckforge/api/schemas/responses.py with:
       - HealthResponse(BaseModel): status: Literal["healthy", "degraded"], checks: dict[str, str], version: str
       - RenderResponse(BaseModel): id: str, status: str, ir: dict | None = None, download_url: str | None = None, quality_score: int | None = None
       - JobResponse(BaseModel): id: str, status: str, progress: float, job_type: str, created_at: str, result: dict | None = None

    3. Create src/deckforge/api/deps.py with dependency injection:
       - DbSession = Annotated[AsyncSession, Depends(get_db)]
       - get_redis(request: Request) -> Redis (from app.state.redis)
       - RedisClient = Annotated[Redis, Depends(get_redis)]

    4. Create src/deckforge/api/middleware/auth.py per research Pattern 5:
       - API_KEY_HEADER = APIKeyHeader(name="X-API-Key", auto_error=False)
       - get_api_key(db: DbSession, api_key: str | None = Security(API_KEY_HEADER)) -> ApiKey:
         Validates dk_live_/dk_test_ prefix, hashes with SHA-256, looks up in db via api_key_repo.get_by_hash.
         Returns 401 for missing, invalid format, or unknown keys.
         Updates last_used_at on successful auth.
       - CurrentApiKey = Annotated[ApiKey, Depends(get_api_key)]

    5. Create src/deckforge/api/middleware/rate_limit.py per research Pattern 6:
       - TOKEN_BUCKET_SCRIPT Lua script (atomic refill + consume)
       - TIER_LIMITS dict: starter 10/min, pro 60/min, enterprise 600/min
       - check_rate_limit(redis, api_key_id: str, tier: str) -> tuple[bool, int, int] (allowed, remaining, retry_after_ms)
       - rate_limit_dependency(redis: RedisClient, api_key: CurrentApiKey) -> None:
         Calls check_rate_limit. If not allowed, raises HTTPException(429) with Retry-After and X-RateLimit-Remaining headers.
       - RateLimited = Annotated[None, Depends(rate_limit_dependency)]

    6. Create src/deckforge/api/routes/health.py:
       - GET /health returning HealthResponse with PostgreSQL and Redis connectivity checks
       - No authentication required on health endpoint

    7. Create src/deckforge/api/routes/render.py:
       - POST /render accepting Presentation model as body, requires auth + rate limit
       - Accepts optional X-Request-Id header for idempotency
       - If X-Request-Id provided, check deck_repo.get_by_request_id first. If found, return existing result.
       - Otherwise, validate IR (Pydantic does this automatically), store in decks table via deck_repo.create, return the parsed IR echoed back as JSON with deck id and status "validated"
       - Invalid IR returns 422 with Pydantic validation errors formatted per ValidationErrorResponse

    8. Create src/deckforge/api/routes/jobs.py:
       - GET /jobs/{job_id} returning JobResponse from job_repo
       - Returns 404 if job not found

    9. Create src/deckforge/api/app.py with create_app() factory:
       - Includes health_router (no prefix beyond /v1), render_router, jobs_router
       - All under /v1 prefix

    10. Create src/deckforge/main.py with lifespan:
        - Startup: create Redis connection from settings.REDIS_URL, store in app.state.redis, ping to verify
        - Shutdown: close Redis connection
        - app = create_app()
        - Configure FastAPI with title="DeckForge API", version="0.1.0", description, lifespan

    11. Update tests/conftest.py with shared fixtures:
        - async_client: httpx AsyncClient configured with the FastAPI app (using ASGITransport)
        - mock_db_session: AsyncSession mock or in-memory SQLite session
        - mock_redis: fakeredis.aioredis.FakeRedis instance
        - seed_api_key: creates a test user + API key in the db, returns the raw key string
        - Override get_db and get_redis dependencies in the test app

    12. Create tests/integration/test_health.py:
        - Test GET /v1/health returns 200 with status "healthy" and checks for postgres + redis

    13. Create tests/integration/test_auth.py:
        - Test: Request without X-API-Key header returns 401
        - Test: Request with invalid format key (not dk_live_/dk_test_) returns 401
        - Test: Request with unknown key hash returns 401
        - Test: Request with valid seeded key returns 200

    14. Create tests/integration/test_rate_limit.py:
        - Test: Sending requests within tier limit succeeds
        - Test: Exceeding tier limit returns 429 with Retry-After header
        - Use fakeredis for the token bucket Lua script execution

    15. Create tests/integration/test_openapi.py:
        - Test: GET /docs returns 200
        - Test: app.openapi() generates schema without error
        - Test: OpenAPI schema contains /v1/render path
        - Test: OpenAPI schema contains Presentation model reference

    16. Create tests/integration/test_idempotency.py:
        - Test: POST /v1/render with X-Request-Id stores the request_id
        - Test: Repeated POST with same X-Request-Id returns the original result (not a new deck)
        - Test: POST without X-Request-Id creates a new deck each time
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/integration/test_health.py tests/integration/test_auth.py tests/integration/test_rate_limit.py tests/integration/test_openapi.py tests/integration/test_idempotency.py -x -q</automated>
  </verify>
  <done>
    - GET /v1/health returns healthy status with postgres/redis checks
    - API key auth validates dk_live_/dk_test_ format, hashes, and looks up in DB
    - Missing/invalid keys return 401
    - Rate limiting enforces tier limits (10/min starter, 60/min pro) via Redis token bucket
    - Exceeding rate limit returns 429 with Retry-After header
    - OpenAPI spec generates with all IR models at /docs
    - POST /v1/render validates IR and echoes back validated JSON
    - Invalid IR returns 422 with descriptive Pydantic errors
    - Idempotent operations via X-Request-Id return cached results
  </done>
</task>

<task type="auto">
  <name>Task 2: ARQ worker infrastructure, S3 storage, webhooks, job tracking</name>
  <files>
    src/deckforge/workers/__init__.py
    src/deckforge/workers/settings.py
    src/deckforge/workers/tasks.py
    src/deckforge/workers/storage.py
    src/deckforge/workers/webhooks.py
    tests/integration/test_workers.py
  </files>
  <action>
    1. Create src/deckforge/workers/settings.py per research Pattern 4:
       - REDIS_SETTINGS from arq.connections.RedisSettings using config
       - worker_startup(ctx): Initialize database session factory, Redis client, S3 client in ctx
       - worker_shutdown(ctx): Clean up connections
       - ContentWorkerSettings class: functions=[generate_content], queue_name="arq:content", max_jobs=20, job_timeout=120, redis_settings, on_startup/shutdown
       - RenderWorkerSettings class: functions=[render_presentation], queue_name="arq:render", max_jobs=4, job_timeout=60, redis_settings, on_startup/shutdown

    2. Create src/deckforge/workers/storage.py:
       - get_s3_client() -> boto3 S3 client configured with settings.S3_ENDPOINT_URL, access key, secret key, signature_version s3v4
       - ensure_bucket(client, bucket_name): Create bucket if it doesn't exist
       - upload_file(client, bucket: str, key: str, data: bytes, content_type: str) -> str: Upload file, return the key
       - get_download_url(client, bucket: str, key: str, expires_in: int = 3600) -> str: Generate presigned download URL
       - delete_file(client, bucket: str, key: str) -> None

    3. Create src/deckforge/workers/webhooks.py:
       - deliver_webhook(url: str, payload: dict, max_retries: int = 3) -> bool:
         Async httpx POST to the webhook URL with JSON payload.
         Retry with exponential backoff (1s, 2s, 4s) on failure.
         Return True if delivered, False if all retries exhausted.
         Timeout per request: 10 seconds.

    4. Create src/deckforge/workers/tasks.py:
       - publish_progress(ctx, job_id: str, stage: str, progress: float):
         Publish to Redis channel f"job:{job_id}:progress" with JSON payload {stage, progress, timestamp}
         Also update Job model progress in database
       - render_presentation(ctx, job_id: str, ir_data: dict):
         Stub implementation that:
         a. Updates job status to "running"
         b. Publishes progress "validating" 0.1
         c. Validates IR with Presentation.model_validate(ir_data) -- will be replaced with real rendering in Phase 3
         d. Publishes progress "rendering" 0.5
         e. Creates a placeholder file (JSON of the validated IR as bytes)
         f. Uploads to S3 via storage.upload_file
         g. Updates deck status to "complete" with file_url
         h. Publishes progress "complete" 1.0
         i. If webhook configured, calls deliver_webhook
         j. On error: updates job/deck status to "failed" with error_message
       - generate_content(ctx, job_id: str, prompt: str):
         Stub implementation that:
         a. Updates job status to "running"
         b. Publishes progress events for "parsing", "outlining", "writing", "refining"
         c. Placeholder: creates a minimal IR with a title slide from the prompt
         d. Enqueues render_presentation with the generated IR
         e. On error: updates job status to "failed"

    5. Create tests/integration/test_workers.py:
       - Test: storage.upload_file and get_download_url work with mocked boto3 (use unittest.mock.patch on boto3.client)
       - Test: deliver_webhook sends POST with correct payload (use httpx mock or respx)
       - Test: publish_progress publishes to Redis channel (use fakeredis)
       - Test: render_presentation task validates IR, updates job status through the pipeline
       - Test: generate_content task produces minimal IR and publishes progress events
       Note: These tests mock external services (S3, Redis, DB) to run without Docker. Use fakeredis for Redis, mocked boto3 for S3, and in-memory SQLite for DB.
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/integration/test_workers.py -x -q</automated>
  </verify>
  <done>
    - ContentWorkerSettings and RenderWorkerSettings configure dual ARQ worker pools
    - Content worker uses queue "arq:content" with max_jobs=20 (I/O-bound)
    - Render worker uses queue "arq:render" with max_jobs=4 (CPU-bound)
    - S3 storage helpers upload files and generate presigned download URLs
    - Webhook delivery sends async POST with exponential backoff retries
    - Job progress events publish to Redis pub/sub channels
    - render_presentation stub validates IR, uploads result, updates job status
    - generate_content stub creates minimal IR and enqueues rendering
    - All worker tests pass with mocked external services
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -m pytest tests/ -x -q` -- ALL tests pass (unit + integration)
2. `python -c "from deckforge.main import app; print(app.openapi()['info']['title'])"` -- prints "DeckForge API"
3. The complete Phase 1 success criteria are met:
   - An API client can POST valid IR JSON and receive 200 with parsed IR echoed back
   - Invalid payload returns descriptive 422 error
   - Auth with dk_live_/dk_test_ keys works; invalid keys get 401
   - Rate limiting enforces tier limits
   - OpenAPI spec is auto-generated at /docs
   - Async jobs flow through ARQ workers with progress tracking
</verification>

<success_criteria>
- All 10 remaining requirements (API-08, API-09, API-10, API-11, API-14, WORKER-01 through WORKER-05) have passing tests
- FastAPI app starts and serves /v1/health, /v1/render, /v1/jobs/{id}
- Auth middleware validates API key format, hash lookup, and returns 401 for invalid
- Rate limiter uses Redis Lua token bucket with per-tier limits
- OpenAPI 3.1 spec generates with all IR models documented
- Idempotent POST /v1/render via X-Request-Id returns cached results
- ARQ dual worker pools (content + render) with separate queue names
- S3 storage helpers work with MinIO endpoint
- Webhook delivery fires with retry logic
- Job progress publishes to Redis pub/sub
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ir-schema/01-03-SUMMARY.md`
</output>

---
phase: 01-foundation-ir-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/deckforge/__init__.py
  - src/deckforge/ir/__init__.py
  - src/deckforge/ir/enums.py
  - src/deckforge/ir/presentation.py
  - src/deckforge/ir/metadata.py
  - src/deckforge/ir/brand_kit.py
  - src/deckforge/ir/validators.py
  - src/deckforge/ir/slides/__init__.py
  - src/deckforge/ir/slides/base.py
  - src/deckforge/ir/slides/universal.py
  - src/deckforge/ir/slides/finance.py
  - src/deckforge/ir/elements/__init__.py
  - src/deckforge/ir/elements/base.py
  - src/deckforge/ir/elements/text.py
  - src/deckforge/ir/elements/data.py
  - src/deckforge/ir/elements/visual.py
  - src/deckforge/ir/elements/layout.py
  - src/deckforge/ir/charts/__init__.py
  - src/deckforge/ir/charts/types.py
  - tests/conftest.py
  - tests/unit/__init__.py
  - tests/unit/test_ir_slides.py
  - tests/unit/test_ir_elements.py
  - tests/unit/test_ir_charts.py
  - tests/unit/test_ir_validation.py
  - tests/unit/test_ir_metadata.py
autonomous: true
requirements:
  - IR-01
  - IR-02
  - IR-03
  - IR-04
  - IR-05
  - IR-06

must_haves:
  truths:
    - "A valid IR JSON with any of the 32 slide types parses into the correct Pydantic model via discriminated union on slide_type"
    - "A valid IR JSON with any element type (text, data, visual, layout) parses into the correct element model via discriminated union on type"
    - "A valid IR JSON with any chart subtype parses into the correct chart model via discriminated union on chart_type"
    - "An invalid IR payload (wrong slide_type, missing required field, wrong chart data shape) returns a descriptive Pydantic ValidationError"
    - "generation_options with target_slide_count, density, chart_style, quality_target validates correctly"
    - "metadata with title, purpose, audience, confidentiality, language validates correctly"
  artifacts:
    - path: "src/deckforge/ir/slides/universal.py"
      provides: "23 universal slide type Pydantic models"
      min_lines: 200
    - path: "src/deckforge/ir/slides/finance.py"
      provides: "9 finance slide type Pydantic models"
      min_lines: 80
    - path: "src/deckforge/ir/elements/text.py"
      provides: "Text element models (heading, body_text, bullet_list, etc.)"
      min_lines: 80
    - path: "src/deckforge/ir/elements/data.py"
      provides: "Data element models (table, chart, kpi_card, etc.)"
      min_lines: 60
    - path: "src/deckforge/ir/charts/types.py"
      provides: "All chart subtype models with data schemas"
      min_lines: 150
    - path: "src/deckforge/ir/presentation.py"
      provides: "Top-level Presentation model composing slides, metadata, brand_kit"
      exports: ["Presentation"]
    - path: "tests/unit/test_ir_slides.py"
      provides: "Tests for all 32 slide type validation"
      min_lines: 100
  key_links:
    - from: "src/deckforge/ir/presentation.py"
      to: "src/deckforge/ir/slides/__init__.py"
      via: "slides: list[SlideUnion] field"
      pattern: "SlideUnion"
    - from: "src/deckforge/ir/slides/base.py"
      to: "src/deckforge/ir/elements/__init__.py"
      via: "elements: list[ElementUnion] field"
      pattern: "ElementUnion"
    - from: "src/deckforge/ir/elements/data.py"
      to: "src/deckforge/ir/charts/__init__.py"
      via: "ChartElement.chart_data field uses ChartUnion"
      pattern: "ChartUnion"
    - from: "src/deckforge/ir/slides/__init__.py"
      to: "src/deckforge/ir/enums.py"
      via: "SlideType enum values match Literal discriminators"
      pattern: "Field\\(discriminator"
---

<objective>
Build the complete IR (Intermediate Representation) Pydantic v2 schema -- the backbone of DeckForge. All 32 slide types, all element types, all chart subtypes, metadata, generation_options, and brand_kit as Pydantic discriminated unions with full validation.

Purpose: The IR schema is the single source of truth consumed by every downstream module (layout, rendering, QA, content). It must exist and validate correctly before any other phase can begin.
Output: A fully validated Pydantic v2 IR schema with unit tests proving all 32 slide types, element types, chart subtypes, and cross-field validation work correctly.
</objective>

<execution_context>
@C:/Users/lukep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-ir-schema/01-RESEARCH.md
@.planning/research/STACK.md
@docs/superpowers/specs/2026-03-28-deckforge-design.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold + IR enums, base models, and element types</name>
  <files>
    pyproject.toml
    src/deckforge/__init__.py
    src/deckforge/ir/__init__.py
    src/deckforge/ir/enums.py
    src/deckforge/ir/slides/__init__.py
    src/deckforge/ir/slides/base.py
    src/deckforge/ir/elements/__init__.py
    src/deckforge/ir/elements/base.py
    src/deckforge/ir/elements/text.py
    src/deckforge/ir/elements/data.py
    src/deckforge/ir/elements/visual.py
    src/deckforge/ir/elements/layout.py
    src/deckforge/ir/charts/__init__.py
    src/deckforge/ir/charts/types.py
    tests/__init__.py
    tests/conftest.py
    tests/unit/__init__.py
    tests/unit/test_ir_elements.py
    tests/unit/test_ir_charts.py
  </files>
  <behavior>
    - Test: HeadingElement(type="heading", content={"text": "Hello", "level": "h1"}) validates successfully
    - Test: BulletListElement(type="bullet_list", content={"items": ["a", "b"]}) validates successfully
    - Test: Each text element type (heading, subheading, body_text, bullet_list, numbered_list, callout_box, pull_quote, footnote, label) validates with correct content schema
    - Test: TableElement, ChartElement, KpiCardElement, MetricGroupElement, ProgressBarElement, GaugeElement, SparklineElement validate with correct content
    - Test: ImageElement, IconElement, ShapeElement, DividerElement, SpacerElement, LogoElement, BackgroundElement validate
    - Test: ContainerElement, ColumnElement, RowElement, GridCellElement validate
    - Test: ElementUnion discriminated union routes to correct model based on type field
    - Test: Invalid element type string raises ValidationError with descriptive message
    - Test: Each chart subtype (bar, line, pie, scatter, combo, area, donut, radar, funnel, waterfall, heatmap, sankey, gantt, sunburst, etc.) validates with data model
    - Test: ChartUnion discriminated union routes to correct chart model based on chart_type field
    - Test: Invalid chart_type raises ValidationError
  </behavior>
  <action>
    1. Create pyproject.toml with Python 3.12 requirement, project name "deckforge", and dependencies:
       - fastapi>=0.135.2, uvicorn[standard]>=0.42.0, pydantic>=2.12, pydantic-settings>=2.6
       - sqlalchemy[asyncio]>=2.0.48, alembic>=1.18.4, psycopg[binary]>=3.2
       - arq>=0.27.0, redis>=7.1.1, boto3>=1.36, httpx>=0.28
       - python-multipart, Pillow>=12.1.1, pyyaml>=6.0
       Dev deps: pytest>=8.3, pytest-asyncio>=0.24, pytest-cov>=6.0, httpx>=0.28, fakeredis>=2.25, ruff>=0.9, mypy>=1.14
       Configure [tool.pytest.ini_options] with asyncio_mode = "auto" and testpaths = ["tests"]
       Configure [tool.ruff] with line-length = 100, target-version = "py312"
       Use src layout: [tool.setuptools.packages.find] where = ["src"]

    2. Create src/deckforge/ir/enums.py with:
       - SlideType(str, Enum) with all 32 values: title_slide, agenda, section_divider, key_message, bullet_points, two_column_text, comparison, timeline, process_flow, org_chart, team_slide, quote_slide, image_with_caption, icon_grid, stats_callout, table_slide, chart_slide, matrix, funnel, map_slide, thank_you, appendix, q_and_a (23 universal) + dcf_summary, comp_table, waterfall_chart, deal_overview, returns_analysis, capital_structure, market_landscape, risk_matrix, investment_thesis (9 finance)
       - ElementType(str, Enum) with all values: heading, subheading, body_text, bullet_list, numbered_list, callout_box, pull_quote, footnote, label (text) + table, chart, kpi_card, metric_group, progress_bar, gauge, sparkline (data) + image, icon, shape, divider, spacer, logo, background (visual) + container, column, row, grid_cell (layout)
       - ChartType(str, Enum) with all values: bar, stacked_bar, grouped_bar, horizontal_bar, line, multi_line, area, stacked_area, pie, donut, scatter, bubble, combo, waterfall, funnel, treemap, radar, tornado, football_field, sensitivity_table, heatmap, sankey, gantt, sunburst
       - LayoutHint(str, Enum): full, split_left, split_right, split_top, two_column, three_column, grid_2x2, grid_3x3, centered, title_only, blank
       - Transition(str, Enum): none, fade, slide, push
       - Purpose(str, Enum): board_meeting, investor_update, sales_pitch, training, project_update, strategy, research, deal_memo, ic_presentation, quarterly_review, all_hands, keynote
       - Audience(str, Enum): c_suite, board, investors, team, clients, public
       - Confidentiality(str, Enum): public, internal, confidential, restricted
       - Density(str, Enum): sparse, balanced, dense
       - ChartStyle(str, Enum): minimal, detailed, annotated
       - Emphasis(str, Enum): visual, data, narrative
       - QualityTarget(str, Enum): draft, presentation_ready, board_ready
       - Tone(str, Enum): formal, professional, conversational, bold
       - HeadingLevel(str, Enum): h1, h2, h3

    3. Create src/deckforge/ir/elements/base.py with BaseElement(BaseModel):
       - type: ElementType (overridden by Literal in subclasses)
       - position: Position | None = None (Position model with x, y, width, height as float, all optional)
       - style_overrides: dict | None = None
       Use `from __future__ import annotations` to avoid circular imports.

    4. Create src/deckforge/ir/elements/text.py with content models and element models:
       - HeadingContent(text: str, level: HeadingLevel = HeadingLevel.H1)
       - HeadingElement(BaseElement): type: Literal["heading"], content: HeadingContent
       - SubheadingContent(text: str)
       - SubheadingElement(BaseElement): type: Literal["subheading"], content: SubheadingContent
       - BodyTextContent(text: str, markdown: bool = False)
       - BodyTextElement(BaseElement): type: Literal["body_text"], content: BodyTextContent
       - BulletListContent(items: list[str], style: Literal["disc", "dash", "arrow"] = "disc")
       - BulletListElement(BaseElement): type: Literal["bullet_list"], content: BulletListContent
       - NumberedListContent(items: list[str], start: int = 1)
       - NumberedListElement(BaseElement): type: Literal["numbered_list"], content: NumberedListContent
       - CalloutBoxContent(text: str, style: Literal["info", "warning", "success", "error"] = "info")
       - CalloutBoxElement(BaseElement): type: Literal["callout_box"], content: CalloutBoxContent
       - PullQuoteContent(text: str, attribution: str | None = None)
       - PullQuoteElement(BaseElement): type: Literal["pull_quote"], content: PullQuoteContent
       - FootnoteContent(text: str, number: int | None = None)
       - FootnoteElement(BaseElement): type: Literal["footnote"], content: FootnoteContent
       - LabelContent(text: str)
       - LabelElement(BaseElement): type: Literal["label"], content: LabelContent

    5. Create src/deckforge/ir/charts/types.py with:
       - ChartDataSeries(name: str, values: list[float | int | None]) for all chart data
       - Base chart content models per chart_type with data schemas:
         - BarChartData(chart_type: Literal["bar"], categories: list[str], series: list[ChartDataSeries], ...)
         - Similarly for stacked_bar, grouped_bar, horizontal_bar, line, multi_line, area, stacked_area, pie, donut, scatter, bubble, combo, waterfall, funnel, treemap, radar, tornado, football_field, sensitivity_table, heatmap, sankey, gantt, sunburst
         - Each has chart_type as Literal discriminator and appropriate data fields
       - ChartUnion = Annotated[Union[all chart models], Field(discriminator="chart_type")]

    6. Create src/deckforge/ir/elements/data.py with:
       - TableContent(headers: list[str], rows: list[list[str | float | int | None]], footer_row: list[str] | None = None, ...)
       - TableElement(BaseElement): type: Literal["table"], content: TableContent
       - ChartElement(BaseElement): type: Literal["chart"], chart_data: ChartUnion (uses the chart discriminated union)
       - KpiCardContent(label: str, value: str | float, change: float | None = None, change_direction: Literal["up", "down", "flat"] | None = None)
       - KpiCardElement(BaseElement): type: Literal["kpi_card"], content: KpiCardContent
       - MetricGroupContent(metrics: list[KpiCardContent])
       - MetricGroupElement(BaseElement): type: Literal["metric_group"], content: MetricGroupContent
       - ProgressBarContent(label: str, value: float, max_value: float = 100)
       - ProgressBarElement(BaseElement): type: Literal["progress_bar"], content: ProgressBarContent
       - GaugeContent(label: str, value: float, min_value: float = 0, max_value: float = 100)
       - GaugeElement(BaseElement): type: Literal["gauge"], content: GaugeContent
       - SparklineContent(values: list[float], label: str | None = None)
       - SparklineElement(BaseElement): type: Literal["sparkline"], content: SparklineContent

    7. Create src/deckforge/ir/elements/visual.py with:
       - ImageContent(url: str | None = None, base64: str | None = None, alt_text: str = "", fit: Literal["contain", "cover", "fill"] = "contain")
       - ImageElement(BaseElement): type: Literal["image"], content: ImageContent
       - IconContent(name: str, set: str = "default", color: str | None = None)
       - IconElement(BaseElement): type: Literal["icon"], content: IconContent
       - ShapeContent(shape: Literal["rectangle", "circle", "rounded_rect", "arrow", "line"], fill: str | None = None, stroke: str | None = None)
       - ShapeElement(BaseElement): type: Literal["shape"], content: ShapeContent
       - DividerElement(BaseElement): type: Literal["divider"] (no content needed, optional style)
       - SpacerElement(BaseElement): type: Literal["spacer"] (height in position)
       - LogoContent(url: str | None = None, placement: Literal["top_left", "top_right", "bottom_left", "bottom_right", "center"] = "top_left")
       - LogoElement(BaseElement): type: Literal["logo"], content: LogoContent
       - BackgroundContent(color: str | None = None, image_url: str | None = None, gradient: dict | None = None)
       - BackgroundElement(BaseElement): type: Literal["background"], content: BackgroundContent

    8. Create src/deckforge/ir/elements/layout.py with:
       - ContainerContent(children: list["ElementUnion"] = []) -- forward ref to avoid circular import
       - ContainerElement(BaseElement): type: Literal["container"], content: ContainerContent
       - ColumnElement(BaseElement): type: Literal["column"], content: ContainerContent
       - RowElement(BaseElement): type: Literal["row"], content: ContainerContent
       - GridCellContent(span: int = 1, children: list["ElementUnion"] = [])
       - GridCellElement(BaseElement): type: Literal["grid_cell"], content: GridCellContent

    9. Create src/deckforge/ir/elements/__init__.py composing ElementUnion as Annotated[Union[all element models], Field(discriminator="type")]. Call model_rebuild() on any models with forward references after all imports.

    10. Install dependencies with `pip install -e ".[dev]"` from pyproject.toml.
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/unit/test_ir_elements.py tests/unit/test_ir_charts.py -x -q</automated>
  </verify>
  <done>
    - All 9 text element types validate with correct content schemas
    - All 7 data element types (table, chart, kpi_card, metric_group, progress_bar, gauge, sparkline) validate
    - All 7 visual element types validate
    - All 4 layout element types validate
    - ElementUnion discriminated union correctly routes by type field
    - All 24 chart subtypes validate with data models
    - ChartUnion discriminated union correctly routes by chart_type field
    - Invalid type/chart_type values produce descriptive ValidationError
    - pyproject.toml with all dependencies exists and installs cleanly
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Slide types, Presentation model, metadata, validation</name>
  <files>
    src/deckforge/ir/slides/universal.py
    src/deckforge/ir/slides/finance.py
    src/deckforge/ir/slides/__init__.py
    src/deckforge/ir/metadata.py
    src/deckforge/ir/brand_kit.py
    src/deckforge/ir/presentation.py
    src/deckforge/ir/validators.py
    src/deckforge/ir/__init__.py
    tests/unit/test_ir_slides.py
    tests/unit/test_ir_validation.py
    tests/unit/test_ir_metadata.py
  </files>
  <behavior>
    - Test: TitleSlide(slide_type="title_slide", elements=[...]) validates successfully
    - Test: Each of the 23 universal slide types validates with correct slide_type Literal
    - Test: Each of the 9 finance slide types validates with correct slide_type Literal
    - Test: SlideUnion correctly routes to the right model based on slide_type discriminator
    - Test: Invalid slide_type value produces descriptive ValidationError mentioning valid options
    - Test: Presentation(metadata=..., theme="executive-dark", slides=[TitleSlide(...)]) validates
    - Test: Presentation with generation_options validates all enum fields (density, chart_style, emphasis, quality_target)
    - Test: Presentation with brand_kit (colors, fonts, logo, footer, tone) validates
    - Test: PresentationMetadata with purpose, audience, confidentiality enums validates
    - Test: Missing required fields (e.g. slides missing, metadata.title missing) produce descriptive errors
    - Test: schema_version defaults to "1.0"
    - Test: A complete IR JSON (metadata + brand_kit + theme + slides with mixed types + generation_options) round-trips through Presentation.model_validate_json() and .model_dump_json()
    - Test: Presentation.model_json_schema() generates without error (OpenAPI readiness)
  </behavior>
  <action>
    1. Create src/deckforge/ir/slides/universal.py with 23 slide models:
       Each extends BaseSlide. Each has `slide_type: Literal["<type_name>"] = "<type_name>"`.
       Slide-type-specific optional fields where appropriate:
       - TitleSlide: (common base is sufficient, elements carry the content)
       - AgendaSlide, SectionDividerSlide, KeyMessageSlide: base is sufficient
       - BulletPointsSlide, TwoColumnTextSlide, ComparisonSlide: base
       - TimelineSlide: timeline_direction: Literal["horizontal", "vertical"] = "horizontal"
       - ProcessFlowSlide: flow_direction: Literal["horizontal", "vertical", "circular"] = "horizontal"
       - OrgChartSlide, TeamSlide, QuoteSlide, ImageWithCaptionSlide: base
       - IconGridSlide: columns: int = 3
       - StatsCalloutSlide, TableSlide, ChartSlide, MatrixSlide: base
       - FunnelSlide, MapSlide, ThankYouSlide, AppendixSlide, QAndASlide: base
       Note: Most content is carried by the elements list. Slide models carry layout hints and type-specific config.

    2. Create src/deckforge/ir/slides/finance.py with 9 finance slide models:
       - DcfSummarySlide(slide_type: Literal["dcf_summary"]): optional fields for discount_rate_range, terminal_growth_range
       - CompTableSlide(slide_type: Literal["comp_table"]): optional highlight_column, sort_by
       - WaterfallChartSlide(slide_type: Literal["waterfall_chart"]): show_running_total: bool = True
       - DealOverviewSlide(slide_type: Literal["deal_overview"])
       - ReturnsAnalysisSlide(slide_type: Literal["returns_analysis"])
       - CapitalStructureSlide(slide_type: Literal["capital_structure"])
       - MarketLandscapeSlide(slide_type: Literal["market_landscape"])
       - RiskMatrixSlide(slide_type: Literal["risk_matrix"]): axes_labels: dict | None = None
       - InvestmentThesisSlide(slide_type: Literal["investment_thesis"])

    3. Create src/deckforge/ir/slides/__init__.py with SlideUnion = Annotated[Union[all 32 slide models], Field(discriminator="slide_type")].

    4. Create src/deckforge/ir/metadata.py with:
       - PresentationMetadata(BaseModel): title: str, subtitle: str | None = None, author: str | None = None, company: str | None = None, date: str | None = None, language: str = "en", purpose: Purpose | None = None, audience: Audience | None = None, confidentiality: Confidentiality = Confidentiality.INTERNAL
       - GenerationOptions(BaseModel): target_slide_count: int | tuple[int, int] | None = None, density: Density = Density.BALANCED, chart_style: ChartStyle = ChartStyle.MINIMAL, emphasis: Emphasis = Emphasis.VISUAL, quality_target: QualityTarget = QualityTarget.PRESENTATION_READY

    5. Create src/deckforge/ir/brand_kit.py with:
       - BrandColors(BaseModel): primary: str, secondary: str | None = None, accent: list[str] = [], background: str | None = None, text: str | None = None, muted: str | None = None
       - BrandFonts(BaseModel): heading: str | None = None, body: str | None = None, mono: str | None = None, caption: str | None = None
       - LogoConfig(BaseModel): url: str, placement: Literal["top_left", "top_right", "bottom_left", "bottom_right", "center"] = "top_left", max_width: float | None = None, max_height: float | None = None
       - FooterConfig(BaseModel): text: str | None = None, include_page_numbers: bool = True, include_date: bool = False, include_logo: bool = False
       - BrandKit(BaseModel): colors: BrandColors | None = None, fonts: BrandFonts | None = None, logo: LogoConfig | None = None, footer: FooterConfig | None = None, tone: Tone | None = None

    6. Create src/deckforge/ir/presentation.py with:
       - Presentation(BaseModel): schema_version: Literal["1.0"] = "1.0", metadata: PresentationMetadata, brand_kit: BrandKit | None = None, theme: str = "executive-dark", slides: list[SlideUnion], generation_options: GenerationOptions | None = None
       - model_config = ConfigDict(json_schema_extra={"title": "DeckForge Presentation IR"})

    7. Create src/deckforge/ir/validators.py with cross-field validators:
       - validate_slide_count: If generation_options.target_slide_count is a range tuple, min < max
       - validate_presentation_not_empty: slides list must have at least 1 slide

    8. Create src/deckforge/ir/__init__.py re-exporting: Presentation, PresentationMetadata, GenerationOptions, BrandKit, SlideUnion, ElementUnion, ChartUnion, and all enums. Call model_rebuild() on Presentation to resolve all forward references.

    9. Write comprehensive tests covering all the behaviors listed above.
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/unit/test_ir_slides.py tests/unit/test_ir_validation.py tests/unit/test_ir_metadata.py -x -q</automated>
  </verify>
  <done>
    - All 23 universal slide types validate via SlideUnion discriminated union
    - All 9 finance slide types validate via SlideUnion discriminated union
    - Invalid slide_type raises descriptive ValidationError
    - Presentation model validates with metadata, theme, slides, optional brand_kit and generation_options
    - PresentationMetadata validates purpose, audience, confidentiality enums
    - GenerationOptions validates density, chart_style, emphasis, quality_target
    - BrandKit validates colors, fonts, logo, footer, tone
    - schema_version defaults to "1.0"
    - Cross-field validators (non-empty slides, valid slide count range) work
    - Full Presentation JSON round-trips through model_validate_json/model_dump_json
    - Presentation.model_json_schema() generates without error
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -m pytest tests/unit/ -x -q` -- all unit tests pass
2. `python -c "from deckforge.ir import Presentation; print(Presentation.model_json_schema())"` -- generates OpenAPI-compatible JSON schema without error
3. The IR schema covers all 32 slide types, all element types, all chart subtypes per the design spec
</verification>

<success_criteria>
- All 6 IR requirements (IR-01 through IR-06) have passing tests
- Pydantic discriminated unions route correctly for slides (32 types), elements (27 types), and charts (24 subtypes)
- Invalid payloads produce descriptive validation errors (not generic "validation failed")
- The full IR JSON schema generates for OpenAPI consumption
- pyproject.toml with all project dependencies installs cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ir-schema/01-01-SUMMARY.md`
</output>

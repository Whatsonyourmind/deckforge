---
phase: 01-foundation-ir-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - docker-compose.yml
  - .env.example
  - .env
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/.gitkeep
  - src/deckforge/config.py
  - src/deckforge/db/__init__.py
  - src/deckforge/db/base.py
  - src/deckforge/db/engine.py
  - src/deckforge/db/models/__init__.py
  - src/deckforge/db/models/user.py
  - src/deckforge/db/models/api_key.py
  - src/deckforge/db/models/deck.py
  - src/deckforge/db/models/job.py
  - src/deckforge/db/repositories/__init__.py
  - src/deckforge/db/repositories/api_key.py
  - src/deckforge/db/repositories/deck.py
  - src/deckforge/db/repositories/job.py
  - tests/integration/__init__.py
  - tests/integration/test_db.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "Docker Compose brings up PostgreSQL, Redis, MinIO, API, and worker containers with a single command"
    - "The Dockerfile bundles TrueType fonts (Liberation, DejaVu) for headless text measurement"
    - "SQLAlchemy models create all required tables (users, api_keys, decks, jobs) in PostgreSQL"
    - "Alembic generates and applies migrations from the SQLAlchemy models"
    - "Environment configuration loads from .env with validated Pydantic Settings"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Full local dev stack: API + content-worker + render-worker + PostgreSQL + Redis + MinIO"
      contains: "services:"
    - path: "Dockerfile"
      provides: "Python 3.12 image with fonts and dependencies"
      contains: "fonts-liberation"
    - path: "src/deckforge/db/models/user.py"
      provides: "User SQLAlchemy model"
      contains: "class User"
    - path: "src/deckforge/db/models/api_key.py"
      provides: "ApiKey SQLAlchemy model with hash and scopes"
      contains: "class ApiKey"
    - path: "src/deckforge/db/models/deck.py"
      provides: "Deck SQLAlchemy model with JSONB IR storage"
      contains: "class Deck"
    - path: "src/deckforge/db/models/job.py"
      provides: "Job SQLAlchemy model for async task tracking"
      contains: "class Job"
    - path: "alembic/env.py"
      provides: "Async Alembic configuration importing all models"
      contains: "target_metadata"
    - path: "src/deckforge/config.py"
      provides: "Pydantic Settings for all environment variables"
      exports: ["Settings", "settings"]
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build context for api and worker services"
      pattern: "build: \\."
    - from: "alembic/env.py"
      to: "src/deckforge/db/models/"
      via: "imports all model modules for autogenerate"
      pattern: "from src.deckforge.db.models import"
    - from: "src/deckforge/db/engine.py"
      to: "src/deckforge/config.py"
      via: "database URL from settings"
      pattern: "settings\\.DATABASE_URL"
    - from: "src/deckforge/db/repositories/"
      to: "src/deckforge/db/models/"
      via: "repository classes query model classes"
      pattern: "select\\(.*Model\\)"
---

<objective>
Build the complete infrastructure foundation: Docker Compose local dev stack, Dockerfile with font bundling, SQLAlchemy database models, Alembic migrations, and Pydantic Settings configuration.

Purpose: Every subsequent phase needs a running database, Redis queue, and file storage. This plan establishes that infrastructure so API and worker code can build on top of it.
Output: A working Docker Compose stack, database models with migrations, repository pattern for data access, and centralized configuration.
</objective>

<execution_context>
@C:/Users/lukep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-ir-schema/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configuration, Dockerfile, Docker Compose, and database models</name>
  <files>
    src/deckforge/config.py
    Dockerfile
    docker-compose.yml
    .env.example
    .env
    src/deckforge/db/__init__.py
    src/deckforge/db/base.py
    src/deckforge/db/engine.py
    src/deckforge/db/models/__init__.py
    src/deckforge/db/models/user.py
    src/deckforge/db/models/api_key.py
    src/deckforge/db/models/deck.py
    src/deckforge/db/models/job.py
  </files>
  <action>
    1. Create src/deckforge/config.py using pydantic-settings BaseSettings:
       - DATABASE_URL: str = "postgresql+psycopg://deckforge:deckforge@localhost:5432/deckforge"
       - REDIS_URL: str = "redis://localhost:6379/0"
       - S3_ENDPOINT_URL: str = "http://localhost:9000"
       - S3_ACCESS_KEY: str = "minioadmin"
       - S3_SECRET_KEY: str = "minioadmin"
       - S3_BUCKET: str = "deckforge"
       - API_HOST: str = "0.0.0.0"
       - API_PORT: int = 8000
       - DEBUG: bool = True
       - ENVIRONMENT: Literal["development", "staging", "production"] = "development"
       - model_config = SettingsConfigDict(env_file=".env", env_prefix="DECKFORGE_", case_sensitive=False)
       Export singleton: settings = Settings()

    2. Create .env.example with all config variables documented with comments.
       Create .env with local dev defaults (same as Settings defaults).
       Add .env to .gitignore if not already there. Create .gitignore with: .env, __pycache__, *.pyc, .pytest_cache, .mypy_cache, .ruff_cache, *.egg-info, dist/, build/

    3. Create Dockerfile per research spec:
       - FROM python:3.12-slim
       - Install system deps: fonts-liberation, fonts-dejavu-core, libpq-dev
       - WORKDIR /app
       - COPY pyproject.toml ./
       - RUN pip install --no-cache-dir -e ".[dev]"
       - COPY . .
       - EXPOSE 8000
       - CMD uvicorn src.deckforge.main:app --host 0.0.0.0 --port 8000

    4. Create docker-compose.yml with 6 services per research spec:
       - api: builds from Dockerfile, runs alembic upgrade head then uvicorn with --reload, ports 8000:8000, volumes .:/app, env_file .env, depends_on postgres (healthy) + redis (healthy) + minio (started)
       - content-worker: builds from Dockerfile, runs `arq src.deckforge.workers.settings.ContentWorkerSettings`, env_file .env, depends_on redis + postgres
       - render-worker: builds from Dockerfile, runs `arq src.deckforge.workers.settings.RenderWorkerSettings`, env_file .env, depends_on redis + postgres
       - postgres: image postgres:16-alpine, env POSTGRES_USER/PASSWORD/DB = deckforge, ports 5432:5432, volume pgdata, healthcheck pg_isready
       - redis: image redis:7-alpine, ports 6379:6379, healthcheck redis-cli ping
       - minio: image minio/minio:latest, command server /data --console-address ":9001", ports 9000:9000 + 9001:9001, env MINIO_ROOT_USER/PASSWORD = minioadmin, volume miniodata
       - volumes: pgdata, miniodata

    5. Create src/deckforge/db/base.py with:
       - SQLAlchemy DeclarativeBase subclass (Base) with metadata naming conventions:
         ix_%(column_0_label)s, uq_%(table_name)s_%(column_0_name)s, ck_%(table_name)s_%(constraint_name)s, fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s, pk_%(table_name)s

    6. Create src/deckforge/db/engine.py with:
       - create_async_engine from settings.DATABASE_URL
       - async_session_factory = async_sessionmaker(engine, expire_on_commit=False)
       - get_db() async generator yielding AsyncSession

    7. Create src/deckforge/db/models/user.py:
       - User(Base): __tablename__ = "users"
       - id: Mapped[uuid.UUID] primary key, default uuid4
       - email: Mapped[str] unique, indexed
       - name: Mapped[str | None]
       - is_active: Mapped[bool] default True
       - created_at: Mapped[datetime] server_default=func.now()
       - updated_at: Mapped[datetime | None] onupdate=func.now()

    8. Create src/deckforge/db/models/api_key.py:
       - ApiKey(Base): __tablename__ = "api_keys"
       - id: Mapped[uuid.UUID] primary key, default uuid4
       - user_id: Mapped[uuid.UUID] ForeignKey("users.id")
       - key_hash: Mapped[str] String(64), unique, indexed
       - key_prefix: Mapped[str] String(16) -- for display ("dk_live_abc...")
       - name: Mapped[str] String(100)
       - scopes: Mapped[list[str]] JSONB, default ["read", "generate"]
       - tier: Mapped[str] String(20), default "starter"
       - is_active: Mapped[bool] default True
       - is_test: Mapped[bool] default False
       - created_at: Mapped[datetime] server_default=func.now()
       - last_used_at: Mapped[datetime | None]

    9. Create src/deckforge/db/models/deck.py:
       - Deck(Base): __tablename__ = "decks"
       - id: Mapped[uuid.UUID] primary key, default uuid4
       - request_id: Mapped[str | None] String(64), unique, indexed
       - api_key_id: Mapped[uuid.UUID] ForeignKey("api_keys.id")
       - ir_snapshot: Mapped[dict] JSONB -- stores the full IR
       - status: Mapped[str] String(20), default "pending" (pending/processing/complete/failed)
       - file_url: Mapped[str | None] String(512)
       - quality_score: Mapped[int | None]
       - error_message: Mapped[str | None]
       - created_at: Mapped[datetime] server_default=func.now()
       - completed_at: Mapped[datetime | None]

    10. Create src/deckforge/db/models/job.py:
        - Job(Base): __tablename__ = "jobs"
        - id: Mapped[uuid.UUID] primary key, default uuid4
        - deck_id: Mapped[uuid.UUID | None] ForeignKey("decks.id")
        - api_key_id: Mapped[uuid.UUID] ForeignKey("api_keys.id")
        - job_type: Mapped[str] String(20) -- "render", "generate"
        - queue_name: Mapped[str] String(30) -- "arq:content", "arq:render"
        - status: Mapped[str] String(20), default "queued" (queued/running/complete/failed)
        - progress: Mapped[float] default 0.0
        - result: Mapped[dict | None] JSONB
        - error_message: Mapped[str | None]
        - created_at: Mapped[datetime] server_default=func.now()
        - started_at: Mapped[datetime | None]
        - completed_at: Mapped[datetime | None]

    11. Create src/deckforge/db/models/__init__.py importing all models so Alembic can see them.
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -c "from deckforge.config import settings; print(settings.DATABASE_URL); from deckforge.db.models import User, ApiKey, Deck, Job; print('All models imported successfully')"</automated>
  </verify>
  <done>
    - Settings loads from .env with all required fields
    - Dockerfile exists with python:3.12-slim, fonts-liberation, fonts-dejavu-core
    - docker-compose.yml has 6 services (api, content-worker, render-worker, postgres, redis, minio) with healthchecks
    - All 4 SQLAlchemy models (User, ApiKey, Deck, Job) import without error
    - Database engine and session factory are configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Alembic migrations, repository pattern, and database integration tests</name>
  <files>
    alembic.ini
    alembic/env.py
    alembic/script.py.mako
    alembic/versions/.gitkeep
    src/deckforge/db/repositories/__init__.py
    src/deckforge/db/repositories/api_key.py
    src/deckforge/db/repositories/deck.py
    src/deckforge/db/repositories/job.py
    tests/integration/__init__.py
    tests/integration/test_db.py
  </files>
  <action>
    1. Initialize Alembic with async template. Create alembic.ini with:
       - sqlalchemy.url = postgresql+psycopg://deckforge:deckforge@localhost:5432/deckforge
       - script_location = alembic
       - file_template = %%(year)d_%%(month).2d_%%(day).2d_%%(rev)s_%%(slug)s

    2. Create alembic/env.py with async configuration per research spec:
       - Import ALL model modules from src.deckforge.db.models (user, api_key, deck, job)
       - Set target_metadata = Base.metadata
       - Implement run_async_migrations() with async_engine_from_config and pool.NullPool
       - Support both offline and online modes
       - Use naming conventions from Base for constraint names

    3. Create alembic/script.py.mako as the standard Alembic migration template.

    4. Create repository classes following the repository pattern:
       a. src/deckforge/db/repositories/api_key.py:
          - ApiKeyRepository class with async methods:
          - get_by_hash(session, key_hash: str) -> ApiKey | None
          - get_by_id(session, key_id: uuid.UUID) -> ApiKey | None
          - create(session, user_id, key_hash, key_prefix, name, scopes, tier, is_test) -> ApiKey
          - deactivate(session, key_id) -> bool
          - update_last_used(session, key_id) -> None
       b. src/deckforge/db/repositories/deck.py:
          - DeckRepository class with async methods:
          - get_by_id(session, deck_id) -> Deck | None
          - get_by_request_id(session, request_id: str) -> Deck | None
          - create(session, api_key_id, ir_snapshot, request_id=None) -> Deck
          - update_status(session, deck_id, status, file_url=None, quality_score=None, error=None) -> None
       c. src/deckforge/db/repositories/job.py:
          - JobRepository class with async methods:
          - get_by_id(session, job_id) -> Job | None
          - create(session, api_key_id, job_type, queue_name, deck_id=None) -> Job
          - update_status(session, job_id, status, progress=None, result=None, error=None) -> None
          - get_active_jobs(session, api_key_id) -> list[Job]

    5. Create src/deckforge/db/repositories/__init__.py exporting all repositories as singletons:
       api_key_repo = ApiKeyRepository(), deck_repo = DeckRepository(), job_repo = JobRepository()

    6. Create tests/integration/test_db.py with tests that:
       - Use an in-process SQLite async engine (to avoid needing Docker for CI):
         Create a temporary async SQLite engine, run Base.metadata.create_all, verify all tables created
       - Test that User model creates with all fields
       - Test that ApiKey model creates with JSONB scopes
       - Test that Deck model creates with JSONB ir_snapshot
       - Test that Job model creates with all status fields
       - Test repository create and get_by_id for each repo
       - Test get_by_hash for ApiKeyRepository
       - Test get_by_request_id for DeckRepository

       Note: Use `sqlalchemy.ext.asyncio.create_async_engine("sqlite+aiosqlite:///:memory:")` for test isolation.
       Add aiosqlite to dev dependencies in pyproject.toml.
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/integration/test_db.py -x -q</automated>
  </verify>
  <done>
    - alembic.ini configured with async PostgreSQL URL
    - alembic/env.py imports all models and supports async migrations
    - All 3 repository classes (ApiKeyRepository, DeckRepository, JobRepository) have create, get, update methods
    - Integration tests pass using in-memory SQLite: all models create tables, all CRUD operations work
    - Repository pattern isolates database access from route handlers
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -c "from deckforge.config import settings; from deckforge.db.models import User, ApiKey, Deck, Job; print('Infrastructure ready')"` -- all imports succeed
2. `python -m pytest tests/integration/test_db.py -x -q` -- database model tests pass
3. Docker Compose file references correct images and healthchecks
4. Alembic env.py imports all models for autogenerate
</verification>

<success_criteria>
- Docker Compose defines all 6 services with proper healthchecks and dependency ordering
- Dockerfile bundles Liberation and DejaVu fonts for headless text measurement
- All 4 SQLAlchemy models (User, ApiKey, Deck, Job) create tables with correct columns and constraints
- Alembic is configured for async migrations with all models imported
- Repository pattern provides testable data access layer
- Pydantic Settings loads all configuration from .env
- Integration tests verify model CRUD operations
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-ir-schema/01-02-SUMMARY.md`
</output>

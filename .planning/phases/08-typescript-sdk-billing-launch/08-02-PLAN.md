---
phase: 08-typescript-sdk-billing-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/deckforge/billing/__init__.py
  - src/deckforge/billing/tiers.py
  - src/deckforge/billing/credits.py
  - src/deckforge/billing/stripe_client.py
  - src/deckforge/billing/stripe_webhooks.py
  - src/deckforge/db/models/usage.py
  - src/deckforge/db/models/__init__.py
  - src/deckforge/db/repositories/usage.py
  - src/deckforge/db/repositories/__init__.py
  - src/deckforge/api/routes/billing.py
  - src/deckforge/api/middleware/credits.py
  - src/deckforge/api/schemas/billing_schemas.py
  - src/deckforge/main.py
  - alembic/versions/004_billing_tables.py
  - tests/test_billing.py
autonomous: true
requirements: [BILL-01, BILL-02, BILL-03, BILL-04, BILL-05, BILL-06]
user_setup:
  - service: stripe
    why: "Payment processing for subscriptions and credit billing"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Secret key"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard -> Developers -> Webhooks -> Signing secret"
      - name: STRIPE_STARTER_PRICE_ID
        source: "Stripe Dashboard -> Products -> DeckForge Starter -> Price ID"
      - name: STRIPE_PRO_PRICE_ID
        source: "Stripe Dashboard -> Products -> DeckForge Pro -> Price ID"
    dashboard_config:
      - task: "Create products: DeckForge Starter (free), DeckForge Pro ($79/mo)"
        location: "Stripe Dashboard -> Products -> Create product"
      - task: "Create Billing Meter: deckforge_credit_usage (sum aggregation)"
        location: "Stripe Dashboard -> Billing -> Meters -> Create meter"
      - task: "Create webhook endpoint pointing to /v1/stripe/webhook"
        location: "Stripe Dashboard -> Developers -> Webhooks -> Add endpoint"

must_haves:
  truths:
    - "Three billing tiers exist: Starter (free/50 credits), Pro ($79/500), Enterprise (custom)"
    - "Credit cost is calculated per render: 1 credit per 10 slides, surcharges for finance/NL"
    - "Credits are reserved before render starts and deducted on completion"
    - "Failed renders release reserved credits back to the user"
    - "User can view their usage dashboard (credits remaining, history)"
    - "Overage charges apply at tier-specific rates when credits are exhausted"
  artifacts:
    - path: "src/deckforge/billing/tiers.py"
      provides: "Tier definitions with credit limits and overage rates"
      exports: ["Tier", "TIERS", "get_tier"]
    - path: "src/deckforge/billing/credits.py"
      provides: "Credit reservation, deduction, and release logic"
      exports: ["CreditManager"]
    - path: "src/deckforge/billing/stripe_client.py"
      provides: "Stripe API wrapper for subscriptions and metering"
      exports: ["StripeClient"]
    - path: "src/deckforge/api/middleware/credits.py"
      provides: "FastAPI dependency that checks credits before processing"
      exports: ["CreditCheck"]
    - path: "src/deckforge/db/models/usage.py"
      provides: "UsageRecord and CreditReservation database models"
      exports: ["UsageRecord", "CreditReservation"]
  key_links:
    - from: "src/deckforge/api/middleware/credits.py"
      to: "src/deckforge/billing/credits.py"
      via: "middleware calls CreditManager.reserve() before allowing render"
      pattern: "CreditManager"
    - from: "src/deckforge/billing/credits.py"
      to: "src/deckforge/db/repositories/usage.py"
      via: "atomic credit reservation and deduction via DB"
      pattern: "usage_repo"
    - from: "src/deckforge/billing/credits.py"
      to: "src/deckforge/billing/stripe_client.py"
      via: "reports usage to Stripe meters on deduction"
      pattern: "StripeClient|meter_event"
    - from: "src/deckforge/billing/stripe_webhooks.py"
      to: "src/deckforge/billing/credits.py"
      via: "subscription events update credit limits"
      pattern: "subscription.*active|CreditManager"
---

<objective>
Build the complete billing system: tier definitions, credit reservation/deduction/release, Stripe integration for subscriptions and metered usage, usage dashboard API, and credit check middleware.

Purpose: Billing gates API usage by tier and credits, enabling a sustainable SaaS revenue model.
Output: Billing module with Stripe integration, credit management, usage tracking, and API endpoints.
</objective>

<execution_context>
@C:/Users/lukep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-typescript-sdk-billing-launch/08-RESEARCH.md

Key existing modules:
- src/deckforge/services/cost_estimator.py -- CostEstimator (from 07-02, calculates credit cost from IR)
- src/deckforge/api/middleware/auth.py -- CurrentApiKey dependency
- src/deckforge/db/models/user.py -- User model
- src/deckforge/db/models/api_key.py -- ApiKey model
- src/deckforge/config.py -- Settings with env var loading

<interfaces>
From src/deckforge/services/cost_estimator.py (created in 07-02):
```python
class CostEstimate(BaseModel):
    base_credits: int
    surcharges: dict[str, float]
    total_credits: int
    breakdown: str

class CostEstimator:
    def estimate(self, presentation: Presentation) -> CostEstimate
```

From src/deckforge/db/models/api_key.py:
```python
class ApiKey(Base):
    id: Mapped[uuid.UUID]
    user_id: Mapped[uuid.UUID]
    key_hash: Mapped[str]
    prefix: Mapped[str]
    tier: Mapped[str]  # "starter", "pro", "enterprise"
    permissions: Mapped[dict]  # JSON
```

From src/deckforge/api/middleware/auth.py:
```python
CurrentApiKey = Annotated[ApiKey, Depends(get_current_api_key)]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tier definitions, credit management, and usage DB models</name>
  <files>
    src/deckforge/billing/__init__.py,
    src/deckforge/billing/tiers.py,
    src/deckforge/billing/credits.py,
    src/deckforge/db/models/usage.py,
    src/deckforge/db/models/__init__.py,
    src/deckforge/db/repositories/usage.py,
    src/deckforge/db/repositories/__init__.py,
    alembic/versions/004_billing_tables.py,
    tests/test_billing.py
  </files>
  <behavior>
    - Test: get_tier("starter") returns Tier with credit_limit=50, overage_rate=0.50
    - Test: get_tier("pro") returns Tier with credit_limit=500, overage_rate=0.30
    - Test: CreditManager.reserve() decrements available credits atomically
    - Test: CreditManager.reserve() raises InsufficientCreditsError when balance too low
    - Test: CreditManager.deduct() converts reservation to usage, releases excess
    - Test: CreditManager.release() restores reserved credits (failed job path)
    - Test: Available credits = credit_limit - used - reserved
    - Test: Overage is allowed (used can exceed credit_limit, tracked for billing)
    - Test: UsageRecord correctly tracks period_start for monthly billing
  </behavior>
  <action>
    Create src/deckforge/billing/__init__.py (empty).

    Create src/deckforge/billing/tiers.py:
    - Tier Pydantic model: name (str), display_name (str), credit_limit (int), price_cents (int), overage_rate_cents (int), rate_limit (int -- req/min), stripe_price_id (str | None)
    - TIERS dict:
      - "starter": Tier(name="starter", display_name="Starter", credit_limit=50, price_cents=0, overage_rate_cents=50, rate_limit=10)
      - "pro": Tier(name="pro", display_name="Pro", credit_limit=500, price_cents=7900, overage_rate_cents=30, rate_limit=60)
      - "enterprise": Tier(name="enterprise", display_name="Enterprise", credit_limit=10000, price_cents=0, overage_rate_cents=10, rate_limit=300)
    - get_tier(name: str) -> Tier: lookup from TIERS dict

    Create src/deckforge/db/models/usage.py:
    - UsageRecord(Base): id (UUID), api_key_id (FK), period_start (date), period_end (date), credit_limit (int), credits_used (int, default 0), credits_reserved (int, default 0), tier (str), created_at
    - CreditReservation(Base): id (UUID), api_key_id (FK), usage_record_id (FK), amount (int), status (str: reserved/completed/cancelled/expired), deck_id (UUID | None), created_at, completed_at
    - Unique constraint on (api_key_id, period_start) for UsageRecord

    Update src/deckforge/db/models/__init__.py to import new models.

    Create src/deckforge/db/repositories/usage.py -- UsageRepository:
    - get_or_create_current(session, api_key_id, tier) -> UsageRecord: find record for current billing period (month), create if absent
    - reserve_credits(session, usage_record_id, amount) -> CreditReservation: atomically increment credits_reserved, create reservation record. Uses SELECT FOR UPDATE to prevent race conditions.
    - deduct_credits(session, reservation_id, actual_amount) -> None: decrement credits_reserved by reservation.amount, increment credits_used by actual_amount, set reservation status="completed"
    - release_credits(session, reservation_id) -> None: decrement credits_reserved by reservation.amount, set reservation status="cancelled"
    - get_usage_history(session, api_key_id, limit=12) -> list[UsageRecord]: last N months of usage

    Update src/deckforge/db/repositories/__init__.py with usage_repo singleton.

    Create src/deckforge/billing/credits.py -- CreditManager:
    - __init__(self, usage_repo, stripe_client=None)
    - async reserve(session, api_key_id, tier, estimated_credits) -> CreditReservation:
      1. Get or create current usage record
      2. Calculate available = credit_limit - credits_used - credits_reserved
      3. If estimated_credits > available AND tier is not enterprise (enterprise allows overage): raise InsufficientCreditsError
      4. Call usage_repo.reserve_credits()
      5. Return reservation
    - async deduct(session, reservation_id, actual_credits) -> None:
      1. Call usage_repo.deduct_credits()
      2. If stripe_client configured, report meter event to Stripe
    - async release(session, reservation_id) -> None:
      1. Call usage_repo.release_credits()

    InsufficientCreditsError: custom exception with available and requested fields.

    Create Alembic migration 004_billing_tables.py:
    - Create usage_records table
    - Create credit_reservations table
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/test_billing.py -x -v</automated>
  </verify>
  <done>Tiers defined, CreditManager reserves/deducts/releases credits atomically, InsufficientCreditsError raised when balance too low, usage records track monthly periods</done>
</task>

<task type="auto">
  <name>Task 2: Stripe integration, credit middleware, billing API, and usage dashboard</name>
  <files>
    src/deckforge/billing/stripe_client.py,
    src/deckforge/billing/stripe_webhooks.py,
    src/deckforge/api/middleware/credits.py,
    src/deckforge/api/routes/billing.py,
    src/deckforge/api/schemas/billing_schemas.py,
    src/deckforge/main.py
  </files>
  <action>
    Create src/deckforge/billing/stripe_client.py -- StripeClient:
    - __init__(self, api_key: str): initialize stripe with API key
    - create_customer(email, name, tier) -> stripe.Customer
    - create_subscription(customer_id, price_id) -> stripe.Subscription
    - cancel_subscription(subscription_id) -> None
    - report_usage(customer_id, credits_used) -> None: create Stripe meter event (event_name="deckforge_credit_usage", payload={"value": str(credits_used), "stripe_customer_id": customer_id})
    - create_checkout_session(customer_id, price_id, success_url, cancel_url) -> str: return checkout URL
    - create_portal_session(customer_id, return_url) -> str: return billing portal URL
    - All methods handle stripe.error.StripeError gracefully

    Create src/deckforge/billing/stripe_webhooks.py:
    - verify_stripe_signature(payload, sig_header, webhook_secret) -> stripe.Event
    - handle_stripe_event(event, db_session) -> None:
      - "customer.subscription.created": update user tier, create/update usage record
      - "customer.subscription.updated": update tier if changed
      - "customer.subscription.deleted": downgrade to starter
      - "invoice.payment_succeeded": log payment
      - "invoice.payment_failed": log failure, could downgrade

    Create src/deckforge/api/middleware/credits.py:
    - CreditCheck: FastAPI Depends that:
      1. Gets current API key from auth middleware
      2. Creates CreditManager with usage_repo
      3. Estimates credit cost (from request body IR, using CostEstimator)
      4. Calls credit_manager.reserve()
      5. Stores reservation token in request.state for later deduction
      6. If InsufficientCreditsError: return 402 Payment Required with details
    - CreditDeduct: utility to call after successful render (deduct actual cost)
    - CreditRelease: utility to call on render failure (release reservation)

    Create src/deckforge/api/schemas/billing_schemas.py:
    - UsageSummaryResponse(tier, credit_limit, credits_used, credits_reserved, credits_available, period_start, period_end, overage_rate)
    - UsageHistoryResponse(periods: list[UsagePeriod])
    - UsagePeriod(period_start, credits_used, credit_limit, overage_credits, overage_cost)
    - UpgradeRequest(tier: str)
    - CheckoutResponse(checkout_url: str)
    - PortalResponse(portal_url: str)

    Create src/deckforge/api/routes/billing.py:
    - GET /v1/billing/usage: current period usage summary (BILL-05)
    - GET /v1/billing/usage/history: last 12 months usage history
    - POST /v1/billing/checkout: create Stripe checkout session for tier upgrade
    - POST /v1/billing/portal: create Stripe billing portal session
    - POST /v1/stripe/webhook: Stripe webhook receiver (verifies signature, handles events)

    Wire billing router into main.py.

    NOTE: The credit middleware (CreditCheck) is created but not wired into render/generate endpoints yet. That wiring requires careful integration with the existing render flow and should be done by adding CreditCheck as a dependency to the render and generate route handlers in render.py and generate.py. Add CreditCheck dependency to POST /v1/render and POST /v1/generate route signatures. After successful render, call CreditDeduct. On failure, call CreditRelease.
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -c "from deckforge.billing.stripe_client import StripeClient; from deckforge.api.middleware.credits import CreditCheck; from deckforge.api.routes.billing import router; print(f'Billing routes: {len(router.routes)}')"</automated>
  </verify>
  <done>Stripe client wraps subscription and metering APIs, credit check middleware gates render endpoints, billing dashboard API returns usage stats, Stripe webhook handler processes subscription events</done>
</task>

</tasks>

<verification>
- Tier definitions return correct credit limits and overage rates
- CreditManager reserve/deduct/release cycle works atomically
- Credit check middleware returns 402 when insufficient credits
- Billing dashboard returns current usage and history
- Stripe webhook handler processes subscription lifecycle events
</verification>

<success_criteria>
- Three tiers exist with correct pricing (Starter $0/50cr, Pro $79/500cr, Enterprise custom)
- Credit reservation prevents over-consumption on concurrent renders
- Failed renders release reserved credits
- Usage dashboard shows credits remaining and history
- Stripe integration handles checkout, portal, and webhook events
</success_criteria>

<output>
After completion, create `.planning/phases/08-typescript-sdk-billing-launch/08-02-SUMMARY.md`
</output>

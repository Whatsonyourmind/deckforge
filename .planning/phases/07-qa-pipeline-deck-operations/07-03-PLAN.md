---
phase: 07-qa-pipeline-deck-operations
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/deckforge/db/models/batch_job.py
  - src/deckforge/db/models/webhook_registration.py
  - src/deckforge/db/models/__init__.py
  - src/deckforge/db/repositories/batch.py
  - src/deckforge/db/repositories/webhook.py
  - src/deckforge/db/repositories/__init__.py
  - src/deckforge/api/routes/batch.py
  - src/deckforge/api/routes/webhooks.py
  - src/deckforge/api/schemas/batch_schemas.py
  - src/deckforge/api/schemas/webhook_schemas.py
  - src/deckforge/workers/webhooks.py
  - src/deckforge/workers/tasks.py
  - src/deckforge/main.py
  - alembic/versions/003_batch_webhook_tables.py
  - tests/test_batch_webhooks.py
autonomous: true
requirements: [BATCH-01, BATCH-02, API-12]

must_haves:
  truths:
    - "User can submit a batch render request with multiple IRs and get individual job IDs"
    - "User can submit a batch variations request with one IR and multiple themes"
    - "Batch completion fires a webhook when all sub-jobs finish"
    - "User can register webhooks for specific event types"
    - "Webhook payloads include HMAC-SHA256 signatures for verification"
    - "QA pipeline runs on every render before delivery, populating quality_score in the response"
  artifacts:
    - path: "src/deckforge/api/routes/batch.py"
      provides: "Batch render and batch variations endpoints"
      exports: ["router"]
    - path: "src/deckforge/api/routes/webhooks.py"
      provides: "Webhook registration CRUD endpoints"
      exports: ["router"]
    - path: "src/deckforge/db/models/batch_job.py"
      provides: "BatchJob database model"
      exports: ["BatchJob"]
    - path: "src/deckforge/db/models/webhook_registration.py"
      provides: "WebhookRegistration database model"
      exports: ["WebhookRegistration"]
    - path: "src/deckforge/workers/webhooks.py"
      provides: "Enhanced webhook delivery with HMAC signing and delivery tracking"
      exports: ["deliver_webhook", "sign_webhook_payload"]
  key_links:
    - from: "src/deckforge/api/routes/batch.py"
      to: "src/deckforge/workers/tasks.py"
      via: "enqueues individual render jobs to ARQ for fan-out"
      pattern: "enqueue_job.*render_presentation"
    - from: "src/deckforge/workers/tasks.py"
      to: "src/deckforge/qa/pipeline.py"
      via: "calls QAPipeline.run() after render_pipeline() produces layout results"
      pattern: "QAPipeline"
    - from: "src/deckforge/workers/tasks.py"
      to: "src/deckforge/workers/webhooks.py"
      via: "fires webhook on job completion with signed payload"
      pattern: "deliver_webhook"
    - from: "src/deckforge/workers/webhooks.py"
      to: "src/deckforge/db/repositories/webhook.py"
      via: "looks up registered webhooks for event type"
      pattern: "webhook_repo"
---

<objective>
Build batch render/variations endpoints with ARQ fan-out, webhook registration with HMAC signatures, and wire the QA pipeline into the render path so every deck gets quality-checked before delivery.

Purpose: Batch operations enable bulk deck production. Webhooks enable async notification patterns. QA integration ensures every delivered deck has been quality-verified.
Output: Batch endpoints, webhook registration, HMAC-signed webhook delivery, QA wired into render_pipeline.
</objective>

<execution_context>
@C:/Users/lukep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-qa-pipeline-deck-operations/07-RESEARCH.md
@.planning/phases/07-qa-pipeline-deck-operations/07-01-SUMMARY.md
@.planning/phases/07-qa-pipeline-deck-operations/07-02-SUMMARY.md

Key existing modules:
- src/deckforge/workers/tasks.py -- render_presentation and render_pipeline functions
- src/deckforge/workers/webhooks.py -- existing deliver_webhook with retry
- src/deckforge/qa/pipeline.py -- QAPipeline (from 07-01)
- src/deckforge/db/models/job.py -- Job model
- src/deckforge/db/repositories/job.py -- JobRepository
- src/deckforge/api/deps.py -- DbSession, RedisClient

<interfaces>
From src/deckforge/qa/pipeline.py (created in 07-01):
```python
class QAPipeline:
    def run(self, presentation: Presentation, layout_results: list[LayoutResult], theme: ResolvedTheme | None = None) -> QAReport
```

From src/deckforge/qa/types.py (created in 07-01):
```python
class QAReport(BaseModel):
    score: int
    grade: str
    categories: list[QACategory]
    issues: list[QAIssue]
    fixes: list[QAFix]
```

From src/deckforge/workers/tasks.py:
```python
def render_pipeline(presentation: Presentation) -> bytes
async def render_presentation(ctx, job_id, ir_data, webhook_url=None) -> dict
async def generate_content(ctx, job_id, prompt, ...) -> dict
```

From src/deckforge/workers/webhooks.py:
```python
async def deliver_webhook(url: str, payload: dict, max_retries=3, timeout=10.0) -> bool
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batch and webhook DB models, repositories, and enhanced webhook delivery</name>
  <files>
    src/deckforge/db/models/batch_job.py,
    src/deckforge/db/models/webhook_registration.py,
    src/deckforge/db/models/__init__.py,
    src/deckforge/db/repositories/batch.py,
    src/deckforge/db/repositories/webhook.py,
    src/deckforge/db/repositories/__init__.py,
    src/deckforge/workers/webhooks.py,
    alembic/versions/003_batch_webhook_tables.py,
    tests/test_batch_webhooks.py
  </files>
  <behavior>
    - Test: sign_webhook_payload produces valid HMAC-SHA256 signature
    - Test: sign_webhook_payload signature is verifiable with the same secret
    - Test: sign_webhook_payload with different secret produces different signature
    - Test: BatchJob model stores total_items and completed_items correctly
    - Test: WebhookRegistration model stores url, events list, and secret
    - Test: BatchRepository.increment_completed updates completed_items atomically
    - Test: WebhookRepository.get_by_event returns only webhooks subscribed to that event type
  </behavior>
  <action>
    Create src/deckforge/db/models/batch_job.py:
    - BatchJob(Base): id (UUID pk), api_key_id (FK), total_items (int), completed_items (int, default 0), failed_items (int, default 0), status (str: pending/running/complete/partial_failure/failed), webhook_url (str | None), created_at, completed_at

    Create src/deckforge/db/models/webhook_registration.py:
    - WebhookRegistration(Base): id (UUID pk), api_key_id (FK), url (str), events (JSON list -- e.g. ["render.complete", "batch.complete"]), secret (str -- auto-generated HMAC key), is_active (bool, default True), created_at

    Update src/deckforge/db/models/__init__.py to import both new models.

    Create src/deckforge/db/repositories/batch.py -- BatchRepository:
    - create(session, api_key_id, total_items, webhook_url=None) -> BatchJob
    - get_by_id(session, batch_id) -> BatchJob | None
    - increment_completed(session, batch_id) -> BatchJob: atomically increment completed_items, if completed_items == total_items set status="complete" and completed_at
    - increment_failed(session, batch_id) -> BatchJob: atomically increment failed_items
    - list_by_api_key(session, api_key_id, offset, limit) -> list[BatchJob]

    Create src/deckforge/db/repositories/webhook.py -- WebhookRepository:
    - create(session, api_key_id, url, events, secret=None) -> WebhookRegistration: auto-generate secret if not provided (secrets.token_hex(32))
    - get_by_api_key(session, api_key_id) -> list[WebhookRegistration]
    - get_by_event(session, api_key_id, event_type) -> list[WebhookRegistration]: filter where event_type in events JSON array
    - delete(session, webhook_id) -> None
    - get_by_id(session, webhook_id) -> WebhookRegistration | None

    Update src/deckforge/db/repositories/__init__.py with singleton instances batch_repo and webhook_repo.

    Enhance src/deckforge/workers/webhooks.py:
    - Add sign_webhook_payload(payload_bytes: bytes, secret: str) -> tuple[str, int]: returns (signature, timestamp). Signature format: HMAC-SHA256 of "{timestamp}.{payload}".
    - Update deliver_webhook to:
      1. Accept optional secret parameter
      2. If secret provided, compute signature and add headers: X-DeckForge-Signature (t={ts},v1={hmac}), X-DeckForge-Timestamp
      3. Keep existing retry logic

    Create alembic migration (003_batch_webhook_tables.py):
    - Create batch_jobs table
    - Create webhook_registrations table
    - Add batch_id nullable FK column to jobs table (for linking sub-jobs to batch)
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/test_batch_webhooks.py -x -v</automated>
  </verify>
  <done>BatchJob and WebhookRegistration models created, repositories work, HMAC webhook signing produces verifiable signatures, migration ready</done>
</task>

<task type="auto">
  <name>Task 2: Batch + webhook API routes and wire QA into render pipeline</name>
  <files>
    src/deckforge/api/routes/batch.py,
    src/deckforge/api/routes/webhooks.py,
    src/deckforge/api/schemas/batch_schemas.py,
    src/deckforge/api/schemas/webhook_schemas.py,
    src/deckforge/workers/tasks.py,
    src/deckforge/main.py
  </files>
  <action>
    Create src/deckforge/api/schemas/batch_schemas.py:
    - BatchRenderItem(ir: dict, theme: str | None = None)
    - BatchRenderRequest(items: list[BatchRenderItem], webhook_url: str | None = None)
    - BatchVariationsRequest(ir: dict, themes: list[str], webhook_url: str | None = None)
    - BatchResponse(batch_id: str, jobs: list[dict], total_items: int)
    - BatchStatusResponse(batch_id: str, status: str, total: int, completed: int, failed: int)

    Create src/deckforge/api/schemas/webhook_schemas.py:
    - WebhookCreateRequest(url: str, events: list[str])
    - WebhookResponse(id: str, url: str, events: list[str], secret: str, is_active: bool, created_at: str)
    - WebhookListResponse(webhooks: list[WebhookResponse])

    Create src/deckforge/api/routes/batch.py:
    - POST /v1/batch/render: Create BatchJob, loop through items, create individual Job records with batch_id FK, enqueue each to ARQ. Return BatchResponse with all job IDs.
    - POST /v1/batch/variations: Expand single IR + themes list into N items, then process same as batch/render.
    - GET /v1/batch/{batch_id}: Return batch status (total, completed, failed).

    Create src/deckforge/api/routes/webhooks.py:
    - POST /v1/webhooks: Register webhook (url + events). Auto-generate HMAC secret. Return secret to user (only time it is shown).
    - GET /v1/webhooks: List registered webhooks for api_key.
    - DELETE /v1/webhooks/{webhook_id}: Delete webhook registration.

    **CRITICAL: Wire QA pipeline into render flow.**

    Modify src/deckforge/workers/tasks.py render_pipeline() function:
    - After rendering PPTX bytes, run QA pipeline:
      ```python
      def render_pipeline(presentation: Presentation) -> tuple[bytes, QAReport]:
          theme_registry = ThemeRegistry()
          text_measurer = TextMeasurer()
          layout_engine = LayoutEngine(text_measurer, theme_registry)
          layout_results = layout_engine.layout_presentation(presentation)
          theme = theme_registry.get_theme(presentation.theme, presentation.brand_kit)
          renderer = PptxRenderer()
          pptx_bytes = renderer.render(presentation, layout_results, theme)

          # QA pass
          qa_pipeline = QAPipeline(theme_registry)
          qa_report = qa_pipeline.run(presentation, layout_results, theme)

          return pptx_bytes, qa_report
      ```
    - Update all callers of render_pipeline to handle the tuple return:
      - render_presentation worker task: store qa_report.score as quality_score on deck
      - generate_content worker task: store qa_report.score as quality_score on deck
      - sync render in api/routes/render.py: include quality_score in response
    - After each job completes in render_presentation:
      - Look up registered webhooks for the api_key via webhook_repo.get_by_event()
      - For each registered webhook, call deliver_webhook with the secret for HMAC signing
      - If job has a batch_id: call batch_repo.increment_completed(), if batch is now complete fire batch.complete webhook

    Wire batch and webhook routers into main.py:
    - app.include_router(batch_router, prefix="/v1")
    - app.include_router(webhooks_router, prefix="/v1")
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -c "from deckforge.api.routes.batch import router as b; from deckforge.api.routes.webhooks import router as w; from deckforge.workers.tasks import render_pipeline; print(f'Batch routes: {len(b.routes)}, Webhook routes: {len(w.routes)}')"</automated>
  </verify>
  <done>Batch render/variations endpoints work with ARQ fan-out, webhooks are registrable with HMAC signing, QA pipeline runs on every render populating quality_score, all routers wired into main app</done>
</task>

</tasks>

<verification>
- POST /v1/batch/render creates a batch with individual sub-jobs
- POST /v1/batch/variations expands one IR into N themed renders
- POST /v1/webhooks registers a webhook and returns the HMAC secret
- render_pipeline now returns (bytes, QAReport) and quality_score is stored on decks
- Webhook deliveries include X-DeckForge-Signature header
</verification>

<success_criteria>
- Batch operations fan out to individual ARQ jobs
- Webhook registration stores URL, events, and auto-generated HMAC secret
- Webhook deliveries are signed with HMAC-SHA256
- Every render path (sync, async, batch) runs QA pipeline and stores quality_score
- All new routes are accessible via the FastAPI app
</success_criteria>

<output>
After completion, create `.planning/phases/07-qa-pipeline-deck-operations/07-03-SUMMARY.md`
</output>

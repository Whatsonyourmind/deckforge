---
phase: 07-qa-pipeline-deck-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/deckforge/api/routes/decks.py
  - src/deckforge/api/routes/estimate.py
  - src/deckforge/api/schemas/deck_schemas.py
  - src/deckforge/services/__init__.py
  - src/deckforge/services/deck_ops.py
  - src/deckforge/services/cost_estimator.py
  - src/deckforge/db/repositories/deck.py
  - tests/test_deck_ops.py
autonomous: true
requirements: [DECK-01, DECK-02, DECK-03, DECK-04, DECK-05, DECK-06, DECK-07, API-04]

must_haves:
  truths:
    - "User can list their decks with pagination via GET /v1/decks"
    - "User can retrieve the IR that produced a deck via GET /v1/decks/{id}/ir"
    - "User can append slides to an existing deck and get the re-rendered result"
    - "User can replace a specific slide in an existing deck"
    - "User can reorder slides in an existing deck"
    - "User can retheme an existing deck with a different theme"
    - "POST /v1/estimate returns credit cost for a given IR payload"
  artifacts:
    - path: "src/deckforge/api/routes/decks.py"
      provides: "Deck CRUD + mutation endpoints"
      exports: ["router"]
    - path: "src/deckforge/services/deck_ops.py"
      provides: "Deck operation logic (append, replace, reorder, retheme, export)"
      exports: ["DeckOperations"]
    - path: "src/deckforge/services/cost_estimator.py"
      provides: "Credit cost estimation from IR metadata"
      exports: ["CostEstimator", "CostEstimate"]
    - path: "src/deckforge/api/routes/estimate.py"
      provides: "POST /v1/estimate endpoint"
      exports: ["router"]
  key_links:
    - from: "src/deckforge/api/routes/decks.py"
      to: "src/deckforge/services/deck_ops.py"
      via: "route handlers call DeckOperations methods"
      pattern: "DeckOperations"
    - from: "src/deckforge/services/deck_ops.py"
      to: "src/deckforge/workers/tasks.py"
      via: "calls render_pipeline() to re-render after mutations"
      pattern: "render_pipeline"
    - from: "src/deckforge/services/deck_ops.py"
      to: "src/deckforge/db/repositories/deck.py"
      via: "loads and saves IR snapshots via DeckRepository"
      pattern: "deck_repo"
---

<objective>
Build deck CRUD operations (list, get, delete, get IR), slide mutation operations (append, replace, reorder, retheme, re-export), and credit cost estimation endpoint.

Purpose: Users need to compose and modify existing decks without starting from scratch, and estimate costs before committing credits.
Output: Deck operations service, CRUD + mutation API endpoints, cost estimator.
</objective>

<execution_context>
@C:/Users/lukep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-qa-pipeline-deck-operations/07-RESEARCH.md

Key existing modules:
- src/deckforge/db/repositories/deck.py -- DeckRepository (get_by_id, create, update_status)
- src/deckforge/db/models/deck.py -- Deck model (id, ir_snapshot, status, file_url, quality_score)
- src/deckforge/workers/tasks.py -- render_pipeline() sync function
- src/deckforge/ir/presentation.py -- Presentation model
- src/deckforge/api/middleware/auth.py -- CurrentApiKey dependency
- src/deckforge/api/deps.py -- DbSession, RedisClient dependencies

<interfaces>
From src/deckforge/db/repositories/deck.py:
```python
class DeckRepository:
    async def get_by_id(session, deck_id: uuid.UUID) -> Deck | None
    async def get_by_request_id(session, request_id: str) -> Deck | None
    async def create(session, *, api_key_id, ir_snapshot, request_id=None) -> Deck
    async def update_status(session, deck_id, status, *, file_url=None, quality_score=None, error=None) -> None
```

From src/deckforge/db/models/deck.py:
```python
class Deck(Base):
    id: Mapped[uuid.UUID]
    request_id: Mapped[str | None]
    api_key_id: Mapped[uuid.UUID]
    ir_snapshot: Mapped[dict]  # JSON column
    status: Mapped[str]
    file_url: Mapped[str | None]
    quality_score: Mapped[int | None]
    created_at: Mapped[datetime]
    completed_at: Mapped[datetime | None]
```

From src/deckforge/workers/tasks.py:
```python
def render_pipeline(presentation: Presentation) -> bytes
```

From src/deckforge/ir/presentation.py:
```python
class Presentation(BaseModel):
    schema_version, metadata, brand_kit, theme, slides: list[SlideUnion], generation_options
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deck operations service and cost estimator</name>
  <files>
    src/deckforge/services/__init__.py,
    src/deckforge/services/deck_ops.py,
    src/deckforge/services/cost_estimator.py,
    src/deckforge/db/repositories/deck.py,
    tests/test_deck_ops.py
  </files>
  <behavior>
    - Test: CostEstimator returns 1 credit for 10-slide deck with no finance slides or charts
    - Test: CostEstimator returns 2 credits for 15-slide deck (ceil(15/10))
    - Test: CostEstimator adds 0.5 per finance slide (comp_table, dcf_summary, etc.)
    - Test: CostEstimator adds 0.2 per chart element
    - Test: DeckOperations.append_slides adds slides to IR and re-validates
    - Test: DeckOperations.replace_slide replaces slide at given index
    - Test: DeckOperations.reorder_slides reorders slides per new index list
    - Test: DeckOperations.retheme changes theme in IR
    - Test: DeckOperations.reorder_slides raises ValueError for invalid index list
  </behavior>
  <action>
    Create src/deckforge/services/__init__.py (empty).

    Create src/deckforge/services/cost_estimator.py:
    - CostEstimate Pydantic model: base_credits (int), surcharges (dict[str, float]), total_credits (int), breakdown (str)
    - CostEstimator class with estimate(presentation: Presentation) -> CostEstimate:
      - base = ceil(slide_count / 10)
      - Finance surcharge: count slides with slide_type in FINANCE_TYPES set, multiply by 0.5
      - Chart surcharge: count elements with type == "chart" across all slides, multiply by 0.2
      - NL generation surcharge: +2 if generation_options is not None (indicates NL-generated)
      - Google Slides surcharge: future placeholder (0 for now)
      - total = ceil(base + sum(surcharges))
    - FINANCE_TYPES = {"comp_table", "dcf_summary", "waterfall_chart", "deal_overview", "returns_analysis", "capital_structure", "market_landscape", "investment_thesis", "risk_matrix"}

    Create src/deckforge/services/deck_ops.py:
    - DeckOperations class (stateless, methods accept Presentation and return modified Presentation):
      - append_slides(ir_dict: dict, new_slides: list[dict]) -> dict: add slides to ir_dict["slides"], re-validate via Presentation.model_validate()
      - replace_slide(ir_dict: dict, index: int, new_slide: dict) -> dict: replace ir_dict["slides"][index], validate
      - reorder_slides(ir_dict: dict, new_order: list[int]) -> dict: reorder ir_dict["slides"] per index list, validate. Raise ValueError if indices invalid.
      - retheme(ir_dict: dict, new_theme: str) -> dict: set ir_dict["theme"] = new_theme, validate
      - re_render(ir_dict: dict) -> bytes: call render_pipeline(Presentation.model_validate(ir_dict))

    Extend DeckRepository with new methods:
    - list_by_api_key(session, api_key_id, offset=0, limit=20) -> list[Deck]
    - count_by_api_key(session, api_key_id) -> int
    - soft_delete(session, deck_id) -> None: set status="deleted"
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/test_deck_ops.py -x -v</automated>
  </verify>
  <done>CostEstimator computes correct credit costs, DeckOperations performs all mutation operations on IR dicts with validation</done>
</task>

<task type="auto">
  <name>Task 2: Deck CRUD + mutation API routes and estimate endpoint</name>
  <files>
    src/deckforge/api/routes/decks.py,
    src/deckforge/api/routes/estimate.py,
    src/deckforge/api/schemas/deck_schemas.py,
    src/deckforge/main.py
  </files>
  <action>
    Create src/deckforge/api/schemas/deck_schemas.py with request/response models:
    - DeckListResponse(items: list[DeckSummary], total: int, offset: int, limit: int)
    - DeckSummary(id: str, status: str, theme: str, slide_count: int, quality_score: int | None, created_at: str, download_url: str | None)
    - DeckDetailResponse(id: str, status: str, ir: dict, download_url: str | None, quality_score: int | None, created_at: str)
    - AppendSlidesRequest(slides: list[dict])
    - ReplaceSlidesRequest(slide: dict)
    - ReorderSlidesRequest(order: list[int])
    - RethemeRequest(theme: str)
    - ExportRequest(format: Literal["pptx", "gslides"])
    - CostEstimateRequest(ir: dict | None = None, prompt: str | None = None)
    - CostEstimateResponse(base_credits: int, surcharges: dict[str, float], total_credits: int, breakdown: str)

    Create src/deckforge/api/routes/decks.py:
    - GET /v1/decks -- list decks for authenticated api_key, paginated (offset, limit query params)
    - GET /v1/decks/{deck_id} -- get deck detail including download_url
    - GET /v1/decks/{deck_id}/ir -- get IR snapshot (DECK-02: reproducibility)
    - DELETE /v1/decks/{deck_id} -- soft delete
    - POST /v1/decks/{deck_id}/append -- append slides, re-render, return updated deck
    - PUT /v1/decks/{deck_id}/slides/{index} -- replace slide at index, re-render
    - POST /v1/decks/{deck_id}/reorder -- reorder slides, re-render
    - POST /v1/decks/{deck_id}/retheme -- change theme, re-render
    - POST /v1/decks/{deck_id}/export -- re-export to different format (DECK-03)

    Each mutation endpoint:
    1. Load deck via deck_repo.get_by_id, verify ownership (api_key match)
    2. Call DeckOperations method to modify IR
    3. Call DeckOperations.re_render() to produce new PPTX bytes
    4. Upload to S3 (reuse storage module), update deck record with new file_url and ir_snapshot
    5. Return updated DeckDetailResponse

    Create src/deckforge/api/routes/estimate.py:
    - POST /v1/estimate -- accepts CostEstimateRequest, returns CostEstimateResponse
    - If ir provided: validate and estimate directly
    - If prompt provided: estimate based on default slide count (10) with NL surcharge
    - If neither: return 422

    Wire both routers into src/deckforge/main.py:
    - app.include_router(decks_router, prefix="/v1")
    - app.include_router(estimate_router, prefix="/v1")
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -c "from deckforge.api.routes.decks import router as d; from deckforge.api.routes.estimate import router as e; print(f'Decks routes: {len(d.routes)}, Estimate routes: {len(e.routes)}')"</automated>
  </verify>
  <done>All deck CRUD endpoints are registered, mutation endpoints modify IR and re-render, estimate endpoint returns credit costs, both routers wired into main app</done>
</task>

</tasks>

<verification>
- GET /v1/decks returns paginated list of decks
- GET /v1/decks/{id}/ir returns the IR snapshot
- POST /v1/decks/{id}/append adds slides and returns updated deck
- POST /v1/decks/{id}/retheme changes theme and returns re-rendered deck
- POST /v1/estimate returns correct credit costs for various IR payloads
</verification>

<success_criteria>
- All 7 DECK requirements have corresponding API endpoints
- Cost estimator correctly calculates base + surcharges for finance/chart/NL
- Deck mutations modify IR, re-validate, and re-render
- API routes are wired into the FastAPI application
</success_criteria>

<output>
After completion, create `.planning/phases/07-qa-pipeline-deck-operations/07-02-SUMMARY.md`
</output>

---
phase: 02-layout-engine-theme-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/deckforge/layout/__init__.py
  - src/deckforge/layout/types.py
  - src/deckforge/layout/grid.py
  - src/deckforge/layout/solver.py
  - src/deckforge/layout/text_measurer.py
  - tests/unit/test_layout_grid.py
  - tests/unit/test_text_measure.py
  - tests/unit/test_layout_solver.py
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-05

must_haves:
  truths:
    - "GridSystem computes correct column boundaries, content area, and span widths for 16:9 slides (13.333in x 7.5in)"
    - "TextMeasurer returns bounding box dimensions in inches for single-line and multi-line text with word wrapping"
    - "SlideLayoutSolver accepts kiwisolver constraints, solves them, and returns resolved position dictionaries"
    - "Spacing verification detects misalignment beyond +-2px tolerance"
  artifacts:
    - path: "src/deckforge/layout/types.py"
      provides: "LayoutRegion, BoundingBox, ResolvedPosition, LayoutResult data classes"
      exports: ["LayoutRegion", "BoundingBox", "ResolvedPosition", "LayoutResult"]
    - path: "src/deckforge/layout/grid.py"
      provides: "12-column grid system with margin/gutter calculations"
      exports: ["GridSystem"]
    - path: "src/deckforge/layout/text_measurer.py"
      provides: "Pillow-based text measurement with font caching and DPI conversion"
      exports: ["TextMeasurer"]
    - path: "src/deckforge/layout/solver.py"
      provides: "Kiwisolver wrapper that manages constraint addition and variable solving"
      exports: ["SlideLayoutSolver"]
  key_links:
    - from: "src/deckforge/layout/text_measurer.py"
      to: "Pillow ImageFont"
      via: "ImageFont.truetype() + getbbox/multiline_textbbox"
      pattern: "ImageFont\\.truetype"
    - from: "src/deckforge/layout/solver.py"
      to: "kiwisolver"
      via: "kiwisolver.Solver + Variable + Constraint"
      pattern: "kiwisolver\\.(Solver|Variable)"
    - from: "src/deckforge/layout/grid.py"
      to: "src/deckforge/layout/types.py"
      via: "uses LayoutRegion for region creation helpers"
      pattern: "from.*types.*import"
---

<objective>
Build the layout engine foundation: type definitions, 12-column grid system, Pillow-based text measurer, and kiwisolver constraint solver wrapper.

Purpose: These are the low-level building blocks that all layout patterns and the LayoutEngine orchestrator (Plan 03) depend on. Without accurate text measurement and a working constraint solver, no layout can be computed.

Output: Four production modules in `src/deckforge/layout/` plus comprehensive unit tests.
</objective>

<execution_context>
@C:/Users/lukep/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-engine-theme-system/02-RESEARCH.md

@src/deckforge/ir/elements/base.py
@src/deckforge/ir/enums.py
@src/deckforge/ir/brand_kit.py

<interfaces>
<!-- Existing IR types the layout engine must integrate with -->

From src/deckforge/ir/elements/base.py:
```python
class Position(BaseModel):
    x: float | None = None
    y: float | None = None
    width: float | None = None
    height: float | None = None

class BaseElement(BaseModel):
    type: ElementType
    position: Position | None = None
    style_overrides: dict | None = None
```

From src/deckforge/ir/enums.py:
```python
class SlideType(str, Enum):
    TITLE_SLIDE = "title_slide"
    BULLET_POINTS = "bullet_points"
    TWO_COLUMN_TEXT = "two_column_text"
    CHART_SLIDE = "chart_slide"
    TABLE_SLIDE = "table_slide"
    # ... 32 total types

class LayoutHint(str, Enum):
    FULL = "full"
    SPLIT_LEFT = "split_left"
    TWO_COLUMN = "two_column"
    # ... etc
```

From src/deckforge/ir/slides/base.py:
```python
class BaseSlide(BaseModel):
    slide_type: SlideType
    elements: list[ElementUnion] = []
    layout_hint: LayoutHint | None = None
    transition: Transition | None = None
    speaker_notes: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layout types, GridSystem, and SlideLayoutSolver</name>
  <files>
    src/deckforge/layout/__init__.py,
    src/deckforge/layout/types.py,
    src/deckforge/layout/grid.py,
    src/deckforge/layout/solver.py,
    tests/unit/test_layout_grid.py,
    tests/unit/test_layout_solver.py
  </files>
  <behavior>
    - GridSystem with default 16:9 dimensions (13.333in x 7.5in) computes content_width = 12.333in (0.5in margins each side)
    - GridSystem.column_span_width(0, 6) returns half the content area minus half a gutter
    - GridSystem.column_left(0) returns margin_left (0.5)
    - GridSystem.column_left(6) returns correct position accounting for 6 columns + 5 gutters
    - LayoutRegion("title") creates kiwisolver Variables for left, top, width, height
    - LayoutRegion.right returns left + width (kiwisolver expression)
    - LayoutRegion.bottom returns top + height (kiwisolver expression)
    - SlideLayoutSolver.add_constraint() accepts kiwisolver constraints
    - SlideLayoutSolver.solve() calls solver.updateVariables() and returns dict of region name -> ResolvedPosition
    - SlideLayoutSolver handles UnsatisfiableConstraint by returning an error result (not raising)
    - Spacing verification: ResolvedPosition.is_aligned_to(other, axis, tolerance=2px) checks alignment within tolerance
  </behavior>
  <action>
    Create `src/deckforge/layout/types.py` with:
    - `BoundingBox` dataclass: width_inches, height_inches, min_height (optional float for "strong" constraint sizing)
    - `ResolvedPosition` dataclass: x, y, width, height (all float, in inches). Include `is_aligned_to(other, axis: str, tolerance_inches: float)` method that checks if two positions share the same left/top/right/bottom within tolerance. Tolerance of 2px at 72 DPI = 2/72 = 0.0278 inches.
    - `LayoutResult` dataclass: slide (original), positions (dict[str, ResolvedPosition]), overflow (bool), split_slides (optional list for multi-slide split)
    - `LayoutRegion` class: name (str), creates kiwisolver.Variable for left, top, width, height. Properties right (left+width) and bottom (top+height) return kiwisolver expressions.

    Create `src/deckforge/layout/grid.py` with:
    - `GridSystem` class with SLIDE_WIDTH_INCHES=13.333, SLIDE_HEIGHT_INCHES=7.5, NUM_COLUMNS=12
    - Constructor takes margin_left/right/top/bottom (default 0.5in each) and gutter (default 0.167in)
    - Computes: content_left, content_top, content_width, content_height, content_bottom, content_right, column_width, spacing (0.167in)
    - `column_span_width(start_col, span)` -> width of span columns including internal gutters
    - `column_left(col)` -> left edge of 0-indexed column
    - Constructor accepts a `theme_spacing` dict parameter (optional) that can override margins and gutter from theme data

    Create `src/deckforge/layout/solver.py` with:
    - `SlideLayoutSolver` class wrapping kiwisolver.Solver
    - `add_constraint(constraint)` method
    - `add_constraints(list)` method
    - `solve(regions: list[LayoutRegion])` -> dict[str, ResolvedPosition]. Catches kiwisolver.UnsatisfiableConstraint and returns None (caller decides how to handle).
    - `reset()` method for reuse

    Create `src/deckforge/layout/__init__.py` exporting GridSystem, SlideLayoutSolver, LayoutRegion, BoundingBox, ResolvedPosition, LayoutResult.

    Write tests FIRST (red), then implement (green).
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/unit/test_layout_grid.py tests/unit/test_layout_solver.py -x -v</automated>
  </verify>
  <done>
    GridSystem correctly computes 12-column grid geometry for 16:9 slides.
    LayoutRegion wraps kiwisolver variables with derived right/bottom properties.
    SlideLayoutSolver solves constraints and returns positions; handles infeasible constraints gracefully.
    ResolvedPosition supports alignment verification within 2px tolerance.
    All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TextMeasurer with Pillow font caching and word wrap</name>
  <files>
    src/deckforge/layout/text_measurer.py,
    tests/unit/test_text_measure.py
  </files>
  <behavior>
    - TextMeasurer.measure_text("Hello World", font_name, size_pt=14) returns BoundingBox with width_inches > 0 and height_inches > 0
    - TextMeasurer.measure_text with max_width_inches word-wraps text and returns taller bounding box
    - Multi-line text (containing \n) uses multiline_textbbox, not getbbox
    - Font cache: loading same font twice returns cached object (verify _font_cache dict size)
    - DPI conversion: at 72 DPI, 14pt font produces height approximately 14/72 = 0.194 inches (within 20% for font-specific metrics)
    - Fallback font: if specified font not found, falls back to a default font (DejaVuSans or Liberation Sans) with a warning
    - Safety margin: returned BoundingBox includes 5% safety margin on both dimensions for cross-platform rendering differences
    - measure_bullet_list(items, font_name, size_pt, max_width) returns total bounding box for all items with inter-item spacing
  </behavior>
  <action>
    Create `src/deckforge/layout/text_measurer.py` with:
    - `TextMeasurer` class
    - Constructor takes `font_dir: str | None = None` (auto-detects system font paths: /usr/share/fonts on Linux, C:/Windows/Fonts on Windows, /System/Library/Fonts on macOS)
    - `_font_cache: dict[tuple[str, int], ImageFont.FreeTypeFont]` for caching loaded fonts
    - `MEASUREMENT_DPI = 72` class constant
    - `SAFETY_MARGIN = 0.05` (5%) class constant
    - `_get_font(font_name, size_pt)` -> loads or retrieves cached FreeTypeFont. Uses `_resolve_font_path` to find .ttf file by name. Falls back to DejaVuSans-Regular if not found (log warning via Python logging).
    - `_resolve_font_path(font_name)` -> searches font_dir for matching .ttf file. Maps common names: "Arial" -> "LiberationSans-Regular.ttf", "Montserrat" -> "Montserrat-Regular.ttf", "Open Sans" -> "OpenSans-Regular.ttf". Falls back to first available .ttf in font_dir.
    - `_word_wrap(text, font, max_width_px)` -> wraps text at word boundaries. Uses font.getlength() for width calculation.
    - `measure_text(text, font_name, size_pt, max_width_inches=None)` -> BoundingBox. For single-line: uses font.getbbox(). For multi-line (contains \n or word-wrapped): uses ImageDraw.multiline_textbbox(). Converts px to inches (px / 72). Applies SAFETY_MARGIN multiplier (1.05).
    - `measure_bullet_list(items, font_name, size_pt, max_width_inches, line_spacing=1.4)` -> BoundingBox. Measures each item individually, sums heights with spacing between items.

    Update `src/deckforge/layout/__init__.py` to also export TextMeasurer.

    Tests should use a known system font or Pillow's default font as fallback to avoid CI font dependency issues. Structure tests to gracefully skip if no TrueType fonts are available (pytest.mark.skipif).

    Write tests FIRST (red), then implement (green).
  </action>
  <verify>
    <automated>cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/unit/test_text_measure.py -x -v</automated>
  </verify>
  <done>
    TextMeasurer measures single-line and multi-line text, returning BoundingBox in inches.
    Word wrapping splits text at max_width boundaries.
    Font cache avoids redundant font loading.
    5% safety margin applied to all measurements.
    Fallback font used when specified font not found.
    measure_bullet_list handles lists of items correctly.
    All tests pass.
  </done>
</task>

</tasks>

<verification>
All layout foundation tests pass:
```bash
cd "C:/Users/lukep/Desktop/Projects AI/SlideMaker" && python -m pytest tests/unit/test_layout_grid.py tests/unit/test_text_measure.py tests/unit/test_layout_solver.py -x -v
```

Import check:
```python
from deckforge.layout import GridSystem, SlideLayoutSolver, TextMeasurer, LayoutRegion, BoundingBox, ResolvedPosition, LayoutResult
```
</verification>

<success_criteria>
- GridSystem produces correct geometry for 16:9 (13.333x7.5) slides with 12-column grid
- TextMeasurer returns inch-based BoundingBox values that are non-zero and proportional to text length
- SlideLayoutSolver wraps kiwisolver and gracefully handles infeasible constraints
- LayoutRegion creates proper kiwisolver Variable instances with derived right/bottom expressions
- All unit tests green with no font-dependency failures in CI
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-engine-theme-system/02-01-SUMMARY.md`
</output>
